# Copy this to .env.local (gitignored) and fill in your dev values.
# CI must set real secrets via secrets manager — never commit a populated .env file.

POSTGRES_USER=aisat
POSTGRES_PASSWORD=change-me-in-dev
POSTGRES_DB=aisat

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change-me-in-dev
