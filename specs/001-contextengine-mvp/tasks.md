---
description: "Task list for AISAT-INTEL MVP (Phase 1) implementation"
---

# Tasks: AISAT-INTEL MVP — AI-Powered Shared Second Brain (Phase 1)

**Input**: Design documents from `/specs/001-contextengine-mvp/`

**Prerequisites**: [plan.md](./plan.md) (required), [spec.md](./spec.md) (required for user stories), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/)

**Tests**: Test tasks ARE included. The plan declares TDD NON-NEGOTIABLE (Constitution Principle VI), and every contract in `contracts/` carries explicit "Contract test obligations". Contract/integration tests are written first (Red), must fail, then implementation makes them pass (Green). Integration tests run real backing services via **Testcontainers** (`testcontainers-go` / `testcontainers-python` spin up Postgres/Redis/NATS/Qdrant per run — no shared/mocked infra); critical end-to-end journeys are exercised in the browser via **Playwright**.

**Organization**: Tasks are grouped by user story (US1–US8) to enable independent implementation and testing. Within each story: tests → models → services → endpoints → integration.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story this task belongs to (US1–US8); omitted for Setup / Foundational / Polish
- Exact file paths are included in every task
- **[ID]**: A task ID is a **stable unique label, not an execution-order index**. IDs are never reused or reassigned once issued, so late additions carry letter suffixes (`T051b`) and Stage 11's Polish block (`T122`–`T128`) numbers below Stage 10's US8 block (`T129`–`T143`) because Polish was drafted before US8 was added. **Execution order is defined by [Dependencies & Execution Order](#dependencies--execution-order), never by numeric ordering.** Every ID must be unique across this file — verify with `grep -oE '^- \[[ x]\] T[0-9]+[a-z]?' tasks.md | sort | uniq -d` before committing a change here.

## Path Conventions (from plan.md)

- **Go BFF/gateway/kernel**: `backend-go/` (`kernel/`, `internal/<feature>/{model,dto,errors,service,infra}`, `migrations/`, `tests/`)
- **Python ML/agent tier**: `backend-python/src/` (`routers/`, `services/`, `mcp_server/`, `schemas/`), `backend-python/tests/`, `backend-python/evals/`
- **React SPA**: `frontend/src/` (`features/<feature>/`, `components/`, `lib/`), `frontend/tests/`
- **Infra**: `deploy/` (`docker-compose.yml`, `Caddyfile`), root `Makefile`

---

## Stage 1: Setup (Shared Infrastructure)

**Purpose**: Three-runtime project skeleton, infra services, and tooling

- [ ] T001 Create the three-runtime directory structure per plan.md: `backend-go/{cmd/api,kernel,internal,migrations,tests}`, `backend-python/{src,tests,evals,prompts}`, `frontend/{src,tests}`, `deploy/`
- [ ] T002 [P] Initialize Go module in `backend-go/go.mod` (Go 1.23) with Gin, GORM, nats.go, go-redis, OpenTelemetry, zerolog, Sentry, and `testcontainers-go` (integration-test deps) dependencies
- [ ] T003 [P] Initialize Python project in `backend-python/pyproject.toml` (Python 3.12) with FastAPI, LangGraph, Mem0, BAML, FastMCP, `docker` (the sandbox backend for **local macOS dev and the first DO-droplet deploy**, which is docker-compose — no Kubernetes API there) with `kubernetes` (the `k8s_pod` backend, needed when k3s/EKS lands) and `e2b` (microVM backend, Phase 2) as **optional extras** — the `Sandbox` port keeps all three behind one interface, so only the deployed backend's client need be installed, qdrant-client, `openai` (client pointed at the LLM gateway URL — provider SDKs live in the gateway service, not here), structlog, Langfuse SDK, and `testcontainers` (pytest integration deps). **MarkItDown** and **Crawl4AI/Playwright** are declared as the `convert` / `crawl` **optional-dependency extras** that build the sandbox templates (`deploy/sandbox/templates/`), not the base image (research §24)
- [ ] T004 [P] Initialize React SPA in `frontend/package.json` (React 19, Vite, TypeScript 5.x, native EventSource, PostHog) with Vitest (unit/component) and Playwright (cross-browser E2E) test tooling
- [ ] T005 [P] Create `deploy/docker-compose.yml` with postgres, redis, qdrant, nats, minio (S3), casdoor, caddy, and the **llm-gateway** (LiteLLM on `:4000`, config from `deploy/llm-gateway/`) services
- [ ] T006 [P] Create `deploy/Caddyfile` for reverse proxy, automatic TLS, and static SPA serving in front of the BFF
- [ ] T007 [P] Create root `Makefile` with `up`, `down`, `migrate`, `dev`, `build`, `test`, `lint`, `eval` targets across all three runtimes
- [ ] T008 [P] Configure Go linting/formatting in `backend-go/.golangci.yml` including `depguard` to forbid `kernel/` importing `internal/` (Principle I/II)
- [ ] T009 [P] Configure Python linting/formatting (ruff + black) in `backend-python/pyproject.toml`
- [ ] T010 [P] Configure frontend linting/formatting (eslint + prettier) in `frontend/.eslintrc.cjs` and `frontend/.prettierrc`
- [ ] T010a [P] Configure shared test harnesses: Testcontainers bootstrap helpers (`backend-go/tests/containers/` and `backend-python/tests/conftest.py` fixtures spinning up Postgres/Redis/NATS/Qdrant) and a Playwright config + fixtures in `frontend/tests/e2e/playwright.config.ts`

**Checkpoint**: All three runtimes build empty; `make up` brings up infra services.

---

## Stage 2: Foundational (Blocking Prerequisites)

**Purpose**: Kernel interfaces, platform clients, RLS/migrations, shared middleware, and the LLM/MCP/access-control chokepoints that every user story depends on

**⚠️ CRITICAL**: No user story work can begin until this stage is complete

### Go kernel interfaces & platform clients

- [ ] T011 [P] Define kernel interfaces in `backend-go/kernel/auth.go`, `bus.go`, `storage.go`, `mailer.go` (email-send port, default Resend adapter), `meter.go` (metering ports — `Meter`/`Pricer`/`Ledger`/`LedgerWriter` + opaque `Scope`, per [metering-ports.md](./contracts/metering-ports.md)), `notify.go` (notification ports — `Notifier`/`Channel`/`ChannelRegistry`/`Store`/`PreferenceStore`/`TemplateRenderer` + opaque `Recipient`/`Tenant`/`Topic`, per [notification-ports.md](./contracts/notification-ports.md)), `authz.go` (authorization ports — `Authorizer`/`Policy`/`Lowerer`/`PrincipalResolver` + the backend-agnostic `Predicate` AST + pure `Eval` + opaque `Actor`/`Scope`, per [authorizer-ports.md](./contracts/authorizer-ports.md); Phase 1 binds the single-axis `Policy`, Phase 2 adds the principal conjunct additively), `approval.go` (human-in-the-loop ports — `HumanGate`/`ApprovalStore` + opaque `Subject`/`Approver`/`Kind` + `Decision`, per [approval-ports.md](./contracts/approval-ports.md); the durable, fail-closed, no-spend-while-paused gate reused by note-enrich accept, the long-horizon `interrupt()`, and sensitivity confirm), `flags.go`, `cache.go`, `actor.go` (consumer-defined, no product imports)
- [ ] T011b [P] Contract test for the Casdoor auth flow (per [auth-flow.md](./contracts/auth-flow.md)) in `backend-go/tests/contract/auth_flow_test.go` — the isolated gate the E2E (T127) + isolation integration (T080) only exercise indirectly: **OIDC Authorization Code + PKCE** (S256 `code_challenge` required; missing/`plain` rejected; `state` round-trip enforced, mismatch rejected — AU7/AU8); the BFF-issued **opaque session token** is HttpOnly + Secure + SameSite + Redis-backed and instantly revocable (AU3/EX3), never a JWT in a JS-readable store; device-PAT scope is workspace-bound; and JWT verification **pins the algorithm** against the JWKS (RS256/ES256 only, `alg:none` and HS-confusion rejected, `exp`/`aud`/`iss` enforced — AU1/AU2). Written first (Red), before the T017 Casdoor adapter + T022/T023 middleware make it pass
- [ ] T012 [P] Implement Postgres client + GORM setup in `backend-go/internal/platform/postgres/postgres.go`
- [ ] T013 [P] Implement Redis client (logical DB + key-prefix role separation per research §10) in `backend-go/internal/platform/redis/redis.go`
- [ ] T014 [P] Implement NATS client (publish/subscribe helpers) in `backend-go/internal/platform/nats/nats.go`
- [ ] T015 [P] Implement Qdrant client wrapper in `backend-go/internal/platform/qdrant/qdrant.go`
- [ ] T016 [P] Implement OTel tracer + zerolog logger bootstrap in `backend-go/internal/platform/otel/otel.go` and `backend-go/internal/platform/logger/logger.go`
- [ ] T017 [P] Implement Casdoor `Auth` interface adapter in `backend-go/kernel/identity/casdoor/auth.go` (swappable with jwt/workos)

### Database schema, RLS & shared layer

- [ ] T018 Create migration framework + base migration in `backend-go/migrations/0001_init.sql` (UUID v7 helpers, `SET LOCAL app.workspace_id` convention)
- [ ] T019 Create kernel tables migration in `backend-go/migrations/0002_kernel.sql`: `users`, `workspaces`, `workspace_members`, `invites`, `audit_log`, `api_keys`, `plans`, `subscriptions`, `notifications` (recipient `user_id`, `category`, `priority`, `title`, `body`, `payload` JSONB, `idem_key`, `read_at`, `created_at`, index `(user_id, read_at, created_at)`, `UNIQUE(user_id, idem_key)`), `notification_outbox` (per-channel delivery intent written in the **same tx** as its `notifications` row so a crash between persist and send drops nothing: `notification_id`, `channel`, `attempts`, `next_attempt_at`, `delivered_at`, `last_error`, `UNIQUE(notification_id, channel)`, index `(delivered_at, next_attempt_at)`), `notification_preferences` (`user_id`, `workspace_id`, `category`, `in_app`, `email`, `UNIQUE(user_id, workspace_id, category)`), `email_suppressions` (`email` citext `UNIQUE`, `reason`, `created_at`), `approval_request` (human-in-the-loop gate: recipient `user_id`, `kind`, `subject_type`, `subject_id`, `prompt`, `requested_payload` JSONB, `status`, `decision` JSONB, `resolved_by`, `resolved_at`, `idem_key`, `expires_at`, `created_at`, `UNIQUE(workspace_id, kind, subject_id) WHERE status='pending'`, `UNIQUE(user_id, idem_key)`, index `(user_id, status, created_at)`), `dead_letters` (terminal poison-message store: `workspace_id`, `source_subject`, `dlq_subject`, `payload` JSONB, `dlq_attempts`, `last_error`, `first_failed_at`, `dead_at`), `feature_flags`. **`audit_log` carries the tamper-evident chain columns** `seq` BIGINT + `prev_hash` + `entry_hash` (per-tenant linked history, the `HashChain` durable form of T022c — distinct from the per-row `result_hash`; head lives in Redis at `audit:head:{kind}:{tenant}`), per [audit-ports.md](./contracts/audit-ports.md)
- [ ] T020 Create RLS policies migration in `backend-go/migrations/0003_rls.sql` applying `USING (workspace_id = current_setting('app.workspace_id')::uuid)` to every tenant-scoped table (FR-014, SC-001) — this isolation predicate is the `authz` `SQLLowerer`'s rendering of the `SingleAxisPolicy` scope term (T022a), emitted as DDL from the same `Predicate` the Qdrant filter lowers (T027), never a hand-copied clause; for `notifications` additionally restrict to the recipient via `USING (workspace_id = current_setting('app.workspace_id')::uuid AND user_id = current_setting('app.user_id')::uuid)` (FR-036, SC-012); apply the **same recipient-scoped predicate to `approval_request`** so a human-in-the-loop gate is visible/resolvable only by its approver, never another member or across workspaces (FR-040, SC-014)
- [ ] T020a [P] Create the `sandbox_run` partitioned audit-table migration (`workspace_id`, `user_id`, `template`, `feature`, `exit_code`, `vcpu_seconds`, `wall_ms`, `egress_bytes`, `timed_out`, `result_hash`, `trace_id`, **`seq` BIGINT + `prev_hash` + `entry_hash` (per-tenant tamper-evident chain link, the `HashChain` durable form of T022c)**, partition by `created_at`; **no** file/generated-code bodies — only `result_hash`, matching the `llm_call_log` no-body rule) in `backend-go/migrations/0006_sandbox.sql` per [data-model.md](./data-model.md) — the sandbox analogue of `agent_audit_log`, folded behind the same audit `Recorder` as `Actor{Kind:"sandbox"}` (FR-023, FR-024, research §24, [audit-ports.md](./contracts/audit-ports.md))
- [ ] T021 [P] Implement shared error envelope `{code,message,details}` in `backend-go/internal/shared/errors/errors.go` and DTO helpers in `backend-go/internal/shared/dto/dto.go` (Principle VIII)
- [ ] T022 [P] Implement Tenant middleware (resolves workspace + Actor from JWT/PAT, runs `SET LOCAL app.workspace_id`, `SET LOCAL app.user_id`, and `SET LOCAL app.clearance` — the Actor already carries clearance) in `backend-go/internal/shared/middleware/tenant.go` (FR-004, FR-027, FR-036). Setting `app.clearance` now gives defense-in-depth on relational document reads and is the request-context seam the Phase 2 second access axis extends with `app.principals` + one RLS predicate additively (no new plumbing later) — see [draft-plan.md — Access model](../draft-plan.md#access-model-decided), formalized as the `Authorizer`/`Policy`/`Lowerer` ports in [authorizer-ports.md](./contracts/authorizer-ports.md) (this middleware is the `PrincipalResolver` → `Actor` + SQL-lowering GUC bundle of that contract)
- [ ] T022a [P] Implement the Go `authz` port (per [authorizer-ports.md](./contracts/authorizer-ports.md)) in `backend-go/kernel/authz/` — the `Predicate` AST + pure `Eval`; the Phase-1 **`SingleAxisPolicy`** (`Visibility` = workspace-isolation ∧ `access_level ≤ clearance` ∧ personal-owner, split by `personal`/`workspace` collection; `Permit` for write/admin/share by role + `scope='personal'` ownership, `mirrored`→read-only, with the derived-write `WriteEnvelope`); the **`SQLLowerer`** rendering that one predicate into *both* the T020 RLS policy DDL (scope term) and the library-list repo `WHERE` + `SET LOCAL app.*` GUC bundle (T045); and the **`PrincipalResolver`** the T022 middleware calls to build the `Actor` (fail-closed; agent subject bounded to `min(owner)` and `∩ owner`; human principals `∩ used_principals`). **One predicate, every Go/Postgres enforcement point is a lowering of it** — the isolation+clearance sites cannot drift (SC-001). Phase 2 adds the `allowed_principals` `Or` conjunct + `app.principals` as one additive term, no signature change. `depguard`: `kernel/authz/**` may not import `internal/**` (extraction-ready, same rule as `kernel/metering`)
- [ ] T022b [P] Implement the Go `approval` port (per [approval-ports.md](./contracts/approval-ports.md)) in `backend-go/kernel/approval/` — the `HumanGate`/`ApprovalStore` interfaces + `Subject`/`Approver`/`Kind`/`Decision`/`Handle` domain types, and the Postgres-backed store over `approval_request`: `Create` (idempotent on `(workspace_id, kind, subject_id)` while pending + `idem_key`), `Resolve` (idempotent — first decision wins; verifies the resolver is the authorized `Approver` via the T022a `authz` `Actor`, returns `not_found` for a non-approver), `Get`/`ListPending` (recipient-scoped), and `Expire` (fail-closed sweep wired to the `approval.expire.tick` scheduler seam in `cmd/worker`). Enforces **no side effect + no spend while `pending`** (the caller withholds `billing.deduct`); writes an `audit_log` row on every resolution (FR-040, FR-023, SC-014). `depguard`: `kernel/approval/**` may not import `internal/**` (extraction-ready, same rule as `kernel/metering`/`kernel/notify`)
- [ ] T022c [P] Implement the Go `audit` port (per [audit-ports.md](./contracts/audit-ports.md)) in `backend-go/kernel/audit/` — the `Recorder`/`Sink`/`HashChain`/`AuditQuery` interfaces + opaque `Tenant`/`Actor`/`Action`/`Subject`/`Entry`/`Sealed`/`Receipt` domain types + `SinkRegistry`, and the Phase-1 **`PostgresSink`** over the partitioned `audit_log` **and** `agent_audit_log` tables (kept as its physical partitions): `Record`/`RecordBatch` (redact → seal → append, idempotent on `(tenant, idem_key)`, `security`/`critical` write **synchronously**, `info` may buffer via the `AuditDurability` knob), the pure per-tenant `HashChain.Seal`/`Verify` (monotonic contiguous `Seq` + `PrevHash` link over `audit:head:{tag}`), and the tenant-scoped read side. **No update/delete surface** anywhere; the only removal is `Sink.Prune` (partition `DROP`). One `Recorder`, two ContextEngine bindings — a member action is `Actor{Kind:"user"}`, an AI tool call `{Kind:"agent"}`, a sandbox run `{Kind:"sandbox"}` — never a new table (FR-023, FR-024). `depguard`: `kernel/audit/**` may not import `internal/**` (extraction-ready, same rule as `kernel/metering`/`kernel/notify`/`kernel/approval`)
- [ ] T022d [P] **Audit port conformance** (per the [audit-ports.md](./contracts/audit-ports.md) contract-test skeleton) in `backend-go/tests/contract/audit_ports_test.go`: the pure **`HashChainContract`** (Seq contiguous + monotonic per tenant; each entry links the previous hash; a fresh chain verifies intact; `Verify` detects an edited hash / `Seq` gap / reorder) and the Testcontainers-Postgres **`SinkContract`** (`//go:build integration`: `Append` idempotent on `(tenant, idem_key)` — replay is `Applied=false`, no second row/`Seq`; `Prune` drops old partitions but keeps the in-window entry; reads are RLS tenant-scoped) plus the **append-only** (no update/delete method exists — compile-time), **no-body** (only refs + `ResultHash`, never payloads — FR-024), and **sync-for-security-events** invariants. The conformance gate a swapped `KafkaSink`/`S3AppendSink` or a different `HashChain` is held to (FR-023, FR-024)
- [ ] T023 [P] Implement Actor/auth + request-id + recovery middleware in `backend-go/internal/shared/middleware/auth.go` and `backend-go/internal/shared/middleware/observability.go`
- [ ] T024 Implement app root wiring (build `appCtx`, call each feature's `SetupModule`) in `backend-go/cmd/api/main.go` and shared router in `backend-go/cmd/api/routes.go`; scaffold `backend-go/cmd/relay/main.go` (SSE streaming role) and `backend-go/cmd/worker/main.go` (background role hosting both **scale-out queue-group consumers** — `notify.<ws>` fan-out + `notify.email.<ws>` email worker, N replicas, idempotent — and **single-owner scheduled jobs** — `*.tick`/`*.refresh` + outbox + DLQ sweep, no in-process timers) sharing the same image (Principle IV, research §14, §15)
- [ ] T024a Implement the generic **DLQ sweeper** (single-owner in `cmd/worker`, triggered by `dlq.sweep.tick`): for each message in every `*.dlq.<ws>` subject, re-publish to its owning work subject with `dlq_attempts+1` under an exponential backoff while `dlq_attempts < MAX_DLQ_ATTEMPTS` (default 5), else write it to `dead_letters` and emit a `dlq.dead.count` metric; never reprocesses payloads itself (re-drive only) in `backend-go/internal/platform/dlq/sweeper.go`, wired in `cmd/worker` (research §18)

### Python tier chokepoints (LLM gateway, MCP server, access filter)

- [ ] T024b [P] Deploy the **standalone LLM gateway service** (LiteLLM on `:4000`) via `deploy/docker-compose.yml` + `deploy/llm-gateway/config.yaml`: map aliases (`fast`/`smart`/`embed`/`rerank`) → provider models, hold provider keys, configure multi-key load-balancing + one-hop fallback + circuit breaker; **disable** its built-in budget/spend and response cache; swappable to Bifrost via `LLM_GATEWAY_KIND` (research §21, §4)
- [ ] T024c [P] Deploy the **standalone sandbox tier** per [sandbox-runtime.md](./contracts/sandbox-runtime.md) + `deploy/sandbox/`: template registry (`tmpl-crawl`/`tmpl-convert`/`tmpl-coderun`) + **default-deny egress proxy** (crawl gets the `web_distill` SSRF allowlist, convert gets none); `SANDBOX_URL`/`SANDBOX_KIND` config; the **only** holder of the pool + keys. **The Phase-1 boundary is "hardened container, no microVM"; the kind is per-environment** — `SANDBOX_KIND=service` on the **first DO-droplet deploy** (docker-compose, no Kubernetes API — long-lived sandbox services that mount **no runtime socket at all**; `SANDBOX_RUNTIME=runsc` is the free gVisor upgrade there since the droplet is Linux), `SANDBOX_KIND=docker` on **local macOS dev only**, and `k8s_pod` on k3s/EKS (created through an RBAC-scoped ServiceAccount, **no container-runtime socket anywhere**). No new node group and no metal in either case. **Under `docker` (local macOS dev only), invariant 8 is an active obligation:** the socket goes to the sandbox executor alone, never to `go-api`/`go-relay`/`go-worker`/`py-api`/`py-worker`/`py-mcp`/`llm-gateway`/`crawl`. Hardening is mandatory and contract-tested: `runAsNonRoot`, `readOnlyRootFilesystem`, `capabilities: {drop: [ALL]}`, `seccompProfile: RuntimeDefault`, `automountServiceAccountToken: false`, `resources.limits` from the spec's `vcpu`/`mem_mb`, a **PID cap** (`podPidsLimit` on the sandbox node pool — node-level kubelet config, not a pod field; `--pids-limit` under `SANDBOX_KIND=docker`), plus a `NetworkPolicy` default-deny egress. Cold-start relief is bought **in order, cheapest-and-safest first**: (1) **pre-pull** template images onto sandbox nodes (DaemonSet warmer, or pinned `@sha256` + `IfNotPresent`) — the biggest win, since an uncached pull turns ~2 s into 30 s+; (2) **scheduling headroom** via a negative-priority `PriorityClass` placeholder pod that a real sandbox preempts instantly — no freshness state to track; (3) **measure** (1–2 s is typically enough against a crawl path already spending seconds on fetch + distill); (4) **only then** a pooled claim, and only for `tmpl-crawl`/`tmpl-convert`. If a pool is built it is a lease protocol with four obligations — atomic claim, claimed-never-returns, **failure paths destroy rather than return**, and provably-pristine members — all contract-tested (T026a), because each fails silently and degrades `max_runs=1` into `max_runs=N`. `tmpl-coderun` is **never pooled**. Plus `max_runs_per_sandbox` and a mandatory workspace-boundary recycle. The KVM/`*.metal` node group (`deploy/eks/terraform/sandbox.tf`, `sandbox_enabled` **default false**) stays unprovisioned until Phase 2 (research §24)
- [ ] T025 [P] Contract test for the LLM gateway-client + service (idempotency, clearance-scoped cache, one-hop fallback via the gateway, embed-no-fallback, PII scrub, OpenAI-wire swappability) in `backend-python/tests/contract/test_llm_gateway.py` per [llm-gateway.md](./contracts/llm-gateway.md)
- [ ] T026 Implement the LLM **gateway-client** wrapper (`LLMRequest`/`LLMResponse`; forwards to the standalone gateway `:4000` via `LLM_GATEWAY_URL`; idempotency, budget gate, clearance-scoped semantic cache, PII scrub via the shared `pii_scrub.py` of T123, cost computation + `billing.deduct` emit, trace + `llm_call_log` write — alias resolution/fallback live in the gateway config of T024b, **not** here) in `backend-python/src/services/llm_gateway.py` (FR-024, FR-029, SC-006, research §21)
- [ ] T026a [P] Contract test for the **`Sandbox` port + tier** (egress **default-deny** on `tmpl-convert`; SSRF parity on `tmpl-crawl`; **no** ambient provider key / DB / Qdrant reachability from inside; `timeout_s` kill → `timed_out=true` spends only used compute; a **fork bomb is contained by the PID cap** and does not degrade the node; **exactly one** `sandbox_run` row + **one** `billing.deduct` `operation_type=sandbox.*` per run — Python never writes the ledger; `SANDBOX_KIND` swap changes no caller code; **pod hardening** — non-root, read-only rootfs, all caps dropped, `RuntimeDefault` seccomp, no SA token, else fail closed; **recycle boundary** — a warm sandbox is never handed a job from a different `workspace_id` and dies after `max_runs_per_sandbox`; **pool claim** (if a pool exists) — concurrent claims never collide, a claimed instance leaves the pool before the job starts, an **errored/timed-out/killed** run's instance is destroyed rather than returned (tested on the failure path, where reuse actually leaks in), and members are provably pristine; **`tmpl-coderun` is never pooled**; **no runtime socket** — no rendered manifest mounts `docker.sock`/a CRI socket into api/relay/worker/gateway/mcp, and the sandbox ServiceAccount has no `exec`/`privileged`/cluster-scoped verbs; **no bind mounts** — a built spec has zero host-path volumes for every template; **create-arg allowlist** — a spec carrying a hostile `template`, out-of-bounds caps, or a smuggled mount/capability/`hostNetwork`/`privileged`/`runtimeClassName` is rejected at build time and never reaches a create call) in `backend-python/tests/contract/test_sandbox_runtime.py` per [sandbox-runtime.md](./contracts/sandbox-runtime.md) (SC-001, SC-006)
- [ ] T026b Implement the Python **`Sandbox` port + E2B adapter** (`SandboxSpec`/`SandboxResult`; `run`/`run_code`; stage `files_in` from S3 **clearance-scoped**; hard `vcpu`/`mem_mb`/`timeout_s` caps; compute metering `vcpu_seconds`/`egress_bytes` → `billing.deduct` `operation_type=sandbox.*` via the spend emitter of T098; one `sandbox_run` audit row per run; **no** provider key/DB handed in; swappable on **two orthogonal axes**: `SANDBOX_KIND` — `service` (**DO Phase 1**, long-lived pools)|`oneshot` (per-job destruction with no socket — the DO path for `tmpl-coderun`)|`k8s_pod` (preferred posture, needs k3s/EKS)|`docker` (**local macOS dev only**)|`e2b_selfhost`|`e2b_cloud` — and `SANDBOX_RUNTIME` (`runc` default | `runsc` for gVisor), **resolved per template** so `tmpl-convert` can run on gVisor while `tmpl-crawl` stays on `runc`) in `backend-python/src/services/sandbox/client.py`, with the **first-deploy adapters** in `backend-python/src/services/sandbox/service_.py` (long-lived pool over HTTP) and `oneshot_.py` (one job → exit; `read_only`+tmpfs, process must exit, supervisor holds the bus and forks the untrusted child with no network), plus `docker_.py` for local macOS only (socket held by that executor **alone**, per invariant 8), the `k8s_pod` adapter in `backend-python/src/services/sandbox/pod.py` (Kubernetes API via an **RBAC-scoped ServiceAccount** — `create`/`get`/`delete` on `pods` in the sandbox namespace only, **no socket, no `exec`, no cluster verbs**) landing with k3s/EKS, and the microVM adapter in `backend-python/src/services/sandbox/e2b.py`. **The pod spec is built from a closed allowlist** — no config-, request-, or model-derived value reaches mounts, network mode, capabilities, `securityContext`, `runtimeClassName`, or seccomp; `template` is validated against the enumerated registry and `vcpu`/`mem_mb`/`timeout_s` against typed bounds **before** spec construction; **no host bind mounts for any template** (files move only via S3 staging) (research §24, SC-006, SC-001, invariants 8–12)
- [ ] T026g *(Phase 2)* **Verify the two open OpenSandbox adoption gates BEFORE `SANDBOX_KIND=opensandbox` ships as the `tmpl-coderun` default** — these are acceptance criteria, not follow-ups (research §24 (i); the numbered items below track that section's original question IDs, so the absent **(4)** is intentional — no gate was dropped): **(1) socket posture** — confirm whether its Docker runtime requires a host container-runtime socket; if so the default holds only on its **Kubernetes** runtime and `oneshot` stays the compose path (invariant 8); **(2) `max_runs = 1`** — confirm an explicit *destroy-after-one-job / never-reuse* guarantee. A creation-time `timeout` is **not** equivalent: it bounds how long an instance lives, not how many jobs it serves. Without an explicit never-reuse guarantee this backend is **unusable for `tmpl-coderun`** whatever its other merits, and the default reverts to `oneshot`/`k8s_pod`. **(3) port exposure** — confirm whether it host-maps sandbox ports automatically. Its Docker runtime is reported to publish random container ports to the host (for Jupyter/VNC-style access); that would be **new inbound exposure on the droplet** and contradicts a stated property of the stack — [`docker-compose.prod.yml`](../../deploy/do/docker-compose.prod.yml) asserts *"Only Caddy publishes ports"*. If it does, sandboxes must be reachable only on the internal network, never published. **(5) pool partitioning** — can its warm pool be keyed by an opaque tenant id, or pooling disabled per template? A platform pool keyed by image, not tenant, hands back an instance that last served another workspace and **breaches SC-001 silently**. **(6) custom images** — it must boot our `tmpl-*` images, not stock runtimes: `tmpl-coderun`'s toolkit is a tool contract enumerated in the `run_script` description, and `tmpl-crawl` pins Playwright to the Chromium in its base image. Also read its Kubernetes RBAC scope (a CRD/controller design typically implies a cluster-scoped operator — broader than our own `k8s_pod` adapter needs)
- [ ] T026c *(Phase 2)* [P] Create the `document_versions` migration (`workspace_id` RLS, `document_id` FK, `version_no`, `s3_key`, `content_hash`, `byte_size`, `created_via` (`member_upload`|`agent_edit`), `created_by_user_id`, `agent_run_id`/`sandbox_run_id`/`approval_request_id` FKs, `UNIQUE (document_id, version_no)`) plus `documents.current_version_no` in `backend-go/migrations/0016_document_versions.sql` per [data-model.md](./data-model.md). **Append-only**: a version row's `s3_key` is never overwritten or deleted — that immutability is the whole reason in-place versioning was chosen over an artifact-only write-back (contract invariant 9)
- [ ] T026d *(Phase 2)* Implement the `transform_files` **write-back + re-index** path: on approval, write the new bytes as `document_versions.version_no + 1`, advance `documents.current_version_no`, and **REPLACE** the index — delete the prior version's Qdrant points + `chunks` rows and ingest the new version in the **same idempotent unit** keyed by `document_version.id`. Appending instead leaves stale chunks answering queries from content that no longer exists; a partial delete leaves the document unsearchable — both are silent. `access_level` is **unchanged** by an agent edit (`WriteEnvelope` floor, `min(agent, owner)`), and `created_by_user_id` is the **approving member**, never the agent, so content still changes only through an authenticated human context (FR-004's principle)
- [ ] T026e *(Phase 2)* Implement the **reviewable-diff approval payload** for `transform_files` — the gate MUST present (a) the generated script source, (b) a rendered diff produced by converting **both** the current and candidate bytes through `tmpl-convert` to Markdown and diffing those, and (c) a structured summary (sheets/rows/cells changed) computed **from the two artifacts**. All three derive from bytes the tier holds; **none may be the model's narration** — FR-040 requires the human decision to originate from the approver and not be derived from model output, and an `.xlsx` is not human-readable, so without this the HITL gate degrades into a rubber stamp (contract invariant 9)
- [ ] T026f *(Phase 2)* Contract test the write-back invariants: an approved edit creates exactly one new version and leaves the prior `s3_key` byte-identical and retrievable; the index contains **no** chunk from the superseded version and every chunk from the new one; an agent edit cannot change `access_level`; `created_by_user_id` is the approver; a **rejected** gate leaves the document, its versions, and the index untouched and spends nothing; and **rollback is refused as an agent tool** (member-only — else an agent could launder a rejected edit by restoring a version it authored)
- [ ] T027 [P] Implement the Python **`QdrantLowerer`** — the Qdrant rendering of the [authz](./contracts/authorizer-ports.md) `SingleAxisPolicy` predicate (the same shape the Go `SQLLowerer` renders to the library-list `WHERE`, T022a) — as the access-control pre-filter helper in `backend-python/src/services/retrieval/filter.py` with **two filter builders** (FR-007, SC-001):
  - `personal_filter(workspace_id, user_id)` → `must = [workspace_id == ctx, user_id == requester_user_id]` (personal collection — owner-only, clearance irrelevant)
  - `workspace_filter(workspace_id, access_level)` → `must = [workspace_id == ctx, access_level <= effective_access_level]` (workspace collection — shared docs)
  - **One predicate, two stores**: these builders and the Go `SQLLowerer` library-list `WHERE` are lowerings of the *same* Phase-1 predicate shape; equality across the two stores is proven by T027a, so a clearance/isolation change in one store can never silently diverge from the other (SC-001)
  - **Deny-by-default guard** (the `Lowerer` totality/fail-closed rule): any search invocation reaching Qdrant without a `workspace_id` filter (and, for the workspace collection, an `access_level` bound) MUST raise rather than execute — converting a missing-filter bug into a loud failure instead of a silent cross-tenant leak (SC-001)
  - Both filters must be tested with a member attempting to retrieve another member's personal doc (must return 0 results)
- [ ] T027a [P] **Authorization port conformance** (per the [authorizer-ports.md](./contracts/authorizer-ports.md) contract-test skeleton) — the differential gate that makes the two Phase-1 clearance/isolation sites provably non-divergent (SC-001, release blocker):
  - **`ParityContract`** (integration, Testcontainers): over a shared seeded corpus of documents × actors, assert the Go `SQLLowerer` library-list `WHERE` (T045, run against Postgres), the Python `QdrantLowerer` filter (T027, run against Qdrant), **and the pgvector `RetrievalService` retrieval path (T065a, run against Postgres under RLS)** admit the **identical** doc-id set, and that all three agree with the pure `Eval` oracle — so the SC-001 floor holds identically whether retrieval is served by the Qdrant or the pgvector backend. Shared fixture `backend-go/tests/contract/authz_corpus.json` consumed by `backend-go/tests/contract/authz_ports_test.go` **and** `backend-python/tests/contract/test_authz_parity.py`
  - **`InvariantContract`** (pure, Go): fail-closed on an empty/untrusted actor; read-deny masked as `not_found` (existence privacy); both-conjuncts; no-implicit-admin-read
  - **`ResolverContract`** (integration, Testcontainers): agent `Clearance == min(agent, owner)` + `Principals ⊆ owner`; demotion-follows-on-next-mint; `Principals ⊆ used_principals`; unresolvable identity errors (never a partial permissive `Actor`)
- [ ] T028 Implement FastMCP server bootstrap + per-role `allowed_tools` allowlist dispatch guard in `backend-python/src/mcp_server/server.py` (FR-011, FR-012)
- [ ] T029 [P] Implement FastAPI app entrypoint + NATS subscriber bootstrap in `backend-python/src/main.py`
- [ ] T029a [P] Implement the Python **`Bus` port** (`publish`/`subscribe`/`queue_group`/`redeliver`, the JetStream semantics named in [nats-subjects.md](./contracts/nats-subjects.md) expressed as an interface) in `backend-python/src/platform/bus/port.py`, with **three adapters selected by `bus.kind`**: `jetstream` (Profile A default, wraps the NATS client), `redis_streams` (`XADD`/consumer-groups/`XCLAIM`), and `inprocess` (direct in-process function calls, no broker) in `backend-python/src/platform/bus/{jetstream,redis_streams,inprocess}.py`. Ingestion/worker publishers and subscribers bind the port, never a concrete client, so a single-container Profile B collapses the async bus to `inprocess` with no NATS dependency (agent-runtime.md swap matrix — `Bus` seam; parity conformance in T060d)
- [ ] T030 [P] Implement BAML client scaffold + structlog/Langfuse bootstrap in `backend-python/src/baml_client/__init__.py` and `backend-python/src/observability.py`

### Frontend foundation

- [ ] T031 [P] Implement API client (error envelope handling, `Idempotency-Key`) in `frontend/src/lib/api.ts`
- [ ] T032 [P] Implement typed SSE client per [sse-events.md](./contracts/sse-events.md) in `frontend/src/lib/sse.ts` (incl. the `approval_request` event + `paused`/`resumed` status stages, FR-040; the `ping` keepalive — ignored, never rendered/counted; and the agentic activity events `agent_step` + correlated `tool_use`/`tool_result` re-associated by `id`/`parent_id`, with unknown `agent_step.kind` degrading to a generic row)
- [ ] T033 [P] Implement shared design-system primitives + app shell/router in `frontend/src/components/` and `frontend/src/App.tsx` (WCAG 2.1 AA)

### Vector store bootstrap & seed

- [ ] T034 Create Qdrant collection bootstrap (`personal`, `workspace`) with payload indexes (`workspace_id`, `user_id`, `access_level`, `hot`, `tags`) wired into `make migrate` in `backend-python/src/services/retrieval/bootstrap.py`
- [ ] T034a [P] Create the **pgvector store bootstrap** (the single-store backend of the `RetrievalService` port) in `backend-python/src/services/retrieval/pgvector_bootstrap.py` wired into `make migrate`: a `chunk_embeddings` table (`chunk_id`, `parent_doc_id`, `workspace_id`, `user_id`, `access_level`, `hot`, `tags`, `embedding vector`, `tsv tsvector`) carrying the **same** payload columns the Qdrant payload indexes carry (so the `SQLLowerer` predicate lowers onto it unchanged), a **dense** HNSW index on `embedding`, and a **lexical** GIN index on `tsv` (BM25 companion — `tsvector`, or ParadeDB when available) so the pgvector backend can reproduce Qdrant's dense+lexical hybrid; the table is **RLS-protected by the same `0003_rls.sql` predicate** (T020), making isolation RLS-native on this backend (SC-001)

**Checkpoint**: Foundation ready — kernel interfaces, RLS, middleware, LLM/MCP/filter chokepoints, and frontend shell exist; user stories can now begin.

---

## Stage 3: User Story 1 - Ingest knowledge into a searchable library (Priority: P1) 🎯 MVP

**Goal**: A member uploads files (PDF/DOCX/MD/image) or pastes a link; the system converts, captions images, auto-tags, summarizes, chunks, embeds, indexes, and streams live progress until the item is browsable in the library.

**Independent Test**: Upload a PDF and paste a URL, watch SSE progress to `indexed`, and confirm both appear in `GET /documents` with auto-tags + summary — no query/team features used.

### Tests for User Story 1 ⚠️ (write first, must fail)

- [ ] T035 [P] [US1] Contract test for `POST /ingest/presign` (oversize → `413`, unsupported video/audio → `501`, access_level ≤ caller clearance) in `backend-go/tests/contract/ingest_presign_test.go` per [bff-rest.md](./contracts/bff-rest.md)
- [ ] T036 [P] [US1] Contract test for `POST /notes`, `POST /notes/{id}/enrich`, `GET /notes/{id}/enrich/{streamId}` SSE stages, note accept (`POST /notes/{id}`), and `GET /ingest/{jobId}/status` in `backend-go/tests/contract/ingest_test.go` per [bff-rest.md](./contracts/bff-rest.md)
- [ ] T037 [P] [US1] Contract test for ingestion + enrich NATS subjects (`ingestion.pdf/docx/image`, `enrich.note` → SSRF-guarded crawl **executed in a `tmpl-crawl` sandbox** → `ingestion.crawl` internal step, `audio`→501 stub, embed-outage→`ingestion.dlq`) in `backend-python/tests/contract/test_ingestion_subjects.py` per [nats-subjects.md](./contracts/nats-subjects.md)
- [ ] T037a [P] [US1] Contract test for `ingestion.dlq` drain via `dlq.sweep.tick` — a parked chunk under the cap is re-driven to the embed step with the original model only and re-embeds idempotently (no duplicate Qdrant point); a chunk reaching `MAX_DLQ_ATTEMPTS` lands in `dead_letters` with a `dlq.dead.count` emit and is not re-driven again in `backend-go/tests/contract/dlq_ingestion_test.go` per [nats-subjects.md](./contracts/nats-subjects.md) (research §18, FR-029)
- [ ] T038 [P] [US1] Integration test for ingestion pipeline (PDF → converting → metadata → chunking → embedding → indexed) in `backend-python/tests/integration/test_ingestion_pipeline.py`
- [ ] T039 [P] [US1] Contract test for `GET /documents` and `GET /documents/{id}` (clearance + RLS scoped, image caption present) in `backend-go/tests/contract/documents_test.go`

### Implementation for User Story 1

- [ ] T040 [US1] Create `Document` partitioned table migration (`source_type` incl. `note`, note columns `body`/`source_links[]`/`citations[]`/`enrich_status`, `tags[]`, `summary`, `data_type`, `access_level`, `scope`, server-stamped security fields, partition by `created_at`) in `backend-go/migrations/0010_documents.sql` (FR-004, FR-001)
- [ ] T041 [P] [US1] Implement `Document` model in `backend-go/internal/ingest/model/document.go`
- [ ] T042 [P] [US1] Implement ingest DTOs + errors (presign request/response, oversize, unsupported) in `backend-go/internal/ingest/dto/dto.go` and `backend-go/internal/ingest/errors/errors.go`
- [ ] T043 [US1] Implement presign service (validate `content_length ≤ max_upload_bytes` → 413, reject video/audio → 501, stamp security fields, default access_level to caller clearance, issue S3 presigned PUT) in `backend-go/internal/ingest/service/presign.go` (FR-003, FR-004)
- [ ] T044 [US1] Implement ingestion + enrich orchestration service (S3 event → publish `ingestion.<mime>.<ws>`; `POST /notes/{id}/enrich` → publish `enrich.note.<ws>`; note accept → normal ingestion) in `backend-go/internal/ingest/service/orchestrate.go` (FR-001)
- [ ] T045 [US1] Implement document repository (RLS-scoped list/get/soft-delete) in `backend-go/internal/ingest/infra/repo/document_repo.go` — the clearance-scoped library-list `WHERE` is **generated by the `authz` `SQLLowerer` (T022a), not hand-written**, so it stays in lockstep with the Python Qdrant filter (parity gate: T027a); a `not_found` (never `forbidden`) is returned for a get outside scope/above clearance (existence privacy, SC-001)
- [ ] T046 [US1] Implement ingest + notes HTTP transport (`/ingest/presign`, `/notes`, `/notes/{id}/enrich`, `/notes/{id}/enrich/{streamId}` SSE, `/notes/{id}` accept, `/ingest/note`, `/ingest/{jobId}/status` SSE, `/documents`, `/documents/{id}`, DELETE) + `SetupModule` in `backend-go/internal/ingest/infra/transport/http/handler.go` and `backend-go/internal/ingest/module.go`
- [ ] T047 [P] [US1] Implement ingestion schemas in `backend-python/src/schemas/ingest.py`
- [ ] T048 [US1] Implement ingestion pipeline orchestrator + NATS consumers per MIME, with status emission via Redis pub/sub in `backend-python/src/services/ingestion/pipeline.py` and `backend-python/src/routers/ingest.py`
- [ ] T049 [P] [US1] Implement MarkItDown converter (PDF/DOCX/MD) — running the parse **inside a `tmpl-convert` sandbox** via the `Sandbox` port (T026b), so untrusted-file parsing (a real parser-RCE surface) is isolated in a **network-denied** sandbox instead of in-process on the ingest pod — this is the template that ratchets to gVisor (`SANDBOX_RUNTIME=runsc`, per-template) with `max_runs_per_sandbox=5` and a **fresh subprocess + temp dir per file** (invariant 11) per research §24; returns Markdown to the pipeline — in `backend-python/src/services/ingestion/markitdown.py` (FR-001, research §24)
- [ ] T050 [P] [US1] Implement image captioner via LLM gateway `fast` alias in `backend-python/src/services/ingestion/captioner.py` (FR-002)
- [ ] T051 [P] [US1] Implement shared `web_distill(urls, intent)` capability — SSRF-guarded fetch (https-only; reject private/loopback/link-local/reserved IPs after resolving all A/AAAA; `redirect:error`; bounded size+timeout) → Crawl4AI → distill-against-intent — in `backend-python/src/services/ingestion/web_distill.py` (FR-001). The **Crawl4AI headless-browser fetch runs inside a `tmpl-crawl` sandbox** via the `Sandbox` port (T026b), with the SSRF allowlist enforced at the **egress proxy** + `NetworkPolicy` (defense-in-depth over the in-code guard); the browser lives in the sandbox, not the orchestrator (research §24). Reused by the Phase-1 `web_search` action tool (T072a).
- [ ] T051a [P] [US1] Implement note-enrich worker (consumes `enrich.note.<ws>`: publishes the fetch to `ingestion.crawl.<ws>` for the crawl orchestrator (T051c), LLM-synthesizes a draft from the distilled result, streams `fetching→distilling→drafting→token→done` via Redis pub/sub; draft not persisted) in `backend-python/src/services/ingestion/enrich.py` (FR-001)
- [ ] T051b [US1] Wire the note-enrichment **accept-gate through the `approval` port** (T022b) — the canonical Phase-1 `HumanGate` instance: reaching `drafted` opens `approval_request(kind='enrich_accept', subject={note})`; `POST /notes/{id}` (accept) is the **resolve** that persists body+citations and enters the normal ingestion path; indexing crawled content happens only past the approve, so no external content mutates the index without explicit approval (FR-001, FR-040, SC-014). Extends the accept handler in T046; no draft is indexed before resolve
- [ ] T051c [P] [US1] Implement the **crawl orchestrator** — the `ingestion.crawl.<ws>` consumer that **replaces the retired bespoke crawl image** (no more `Dockerfile.crawl`): a thin role running from the `backend-python` image that executes the `web_distill` fetch (T051) **inside a `tmpl-crawl` sandbox** over the `Sandbox` port (T026b) and returns the distilled draft to the enrich flow; the headless Chromium lives in the sandbox (egress SSRF-allowlisted at the proxy + `NetworkPolicy`), never in the orchestrator process — in `backend-python/src/services/ingestion/crawl_orchestrator.py` (FR-001, research §24)
- [ ] T052 [P] [US1] Implement `ingestion.audio` 501 stub consumer in `backend-python/src/services/ingestion/audio_stub.py` (FR-003)
- [ ] T053 [P] [US1] Implement BAML metadata extractor (advisory tags/data_type/summary/suggested_sensitivity — advisory only) in `backend-python/src/services/ingestion/tagger.py` (FR-002, FR-005)
- [ ] T053a [P] [US1] Wire the **advisory-sensitivity confirm gate** (FR-005, FR-040) in `backend-go/internal/ingest/service/` — when the T053 metadata extractor suggests an `access_level` **above** the uploader's default, open `approval_request(kind='sensitivity_confirm', subject={document})` and keep the uploader default until a member approves; a reject/expire leaves the default (never auto-raises clearance)
- [ ] T054 [US1] Implement **structure-aware** parent/child chunker in `backend-python/src/services/ingestion/chunker.py` (research §16): split on document structure (markdown headings / paragraph / sentence boundaries / table rows / code fences, recursive token-splitter fallback for unstructured text) — never mid-sentence — into a parent/child "small-to-big" scheme (child 200 / parent 1000 tokens, `parent_doc_id` link); child is the embedded/searched unit, parent is sent to the LLM
- [ ] T054a [P] [US1] Implement flag-gated **contextual-retrieval** prefix in the chunker (research §16): when kernel flag `chunking.contextual_prefix` is on (default on for text docs), prepend each child with a short situating line generated via the `fast` alias, computing the per-document summary **once** and reusing it across that doc's chunks to bound cost; prefix is embedded with the child but recorded as non-citable (not a citation span); enabling it in CI is gated on the `evals/run.py` recall/MRR/citation gate (T125)
- [ ] T055 [US1] Implement embed + Qdrant upsert with full payload (incl. `access_level`, `hot`); embed-provider outage → `ingestion.dlq.<ws>` carrying `dlq_attempts`/`first_failed_at` (no model substitution); a sweeper re-drive re-embeds the **same chunk id** idempotently (upsert, not duplicate point) in `backend-python/src/services/ingestion/embed_index.py` (FR-029, research §18)
- [ ] T056 [P] [US1] Implement upload + library UI (drag-drop, presign PUT, live SSE progress, document list with tags/summary, image caption view) in `frontend/src/features/upload/` and `frontend/src/features/library/`

**Checkpoint**: US1 fully functional — ingest PDF/DOCX/MD/image/link, live progress, browsable library. MVP-demoable on its own.

---

## Stage 4: User Story 2 - Ask questions and get access-scoped, cited answers (Priority: P1)

**Goal**: A member asks a natural-language question; the LangGraph agent moderates, rewrites, retrieves (hybrid + rerank) within clearance scope, expands chunks, injects memory, generates a cited answer, and streams it — never surfacing content above clearance or following injected instructions.

**Independent Test**: After ingesting a few docs, ask a question whose answer lives in one; confirm the response cites the correct source and never references docs above clearance; a follow-up uses session context.

### Tests for User Story 2 ⚠️ (write first, must fail)

- [ ] T057 [P] [US2] Contract test for `POST /query` (returns `stream_id`, moderation short-circuit → `injection_blocked`/`disallowed` before spend, idempotency) in `backend-go/tests/contract/query_test.go` per [bff-rest.md](./contracts/bff-rest.md)
- [ ] T058 [P] [US2] Contract test for query SSE taxonomy + the `DebugResponse` section envelope in `backend-go/tests/contract/query_sse_test.go` per [sse-events.md](./contracts/sse-events.md), asserting: the `ping` keepalive is emitted on an idle long-lived stream and is ignored (never a token/ordering/spend effect); every `tool_use` is paired with exactly one `tool_result` of the same `id` with `status:'error'` on failure (correct pairing under interleaved concurrent calls); and every `agent_step` `start` is closed by an `end` under the same `id` with `parent_id` referencing an existing beat, unknown `kind` tolerated (additive-`kind` forward-compat)
- [ ] T059 [P] [US2] Contract test for MCP tools in `backend-python/tests/contract/test_mcp_tools.py` per [mcp-tools.md](./contracts/mcp-tools.md), covering **every category, not just a sample**: (A) `search_workspace_knowledge` never returns `access_level > effective_access_level` (SC-001), `search_personal_knowledge` is **owner-scoped, not clearance-scoped** (a member sees their own personal docs at any clearance; `access_level` is not applied), and `get_document_by_id` is **not existence-probeable** — an out-of-workspace/above-clearance id returns the **identical `404 not_found`** as a nonexistent id so a higher-clearance doc cannot be inferred (SC-001); (B) structured tools reject raw SQL (typed filters only, FR-008); **allowlist enforcement (FR-011)** — a `user`-role agent invoking **any** tool (A–C included, not only `web_search`) absent from its role's `allowed_tools` is rejected **before execution**; **audit (FR-023)** — every **successful** tool call writes **exactly one** `agent_audit_log` row carrying `tool_called`/`token_cost`/`result_hash`/`trace_id`; and **Category-D actions (FR-041)** — `web_search` performs no fetch before its per-search gate approves and is rejected outside the `user`/`admin` allowlist; `edit_note` is rejected when `can_write` is false, above `min(agent,owner)` clearance (`not_found`), when it would raise `access_level` above the source floor (`envelope_widens`), or on a create attempt, and never mutates the stored note before approval (SC-015)
- [ ] T059a [P] [US2] Contract test for the **MCP endpoint's own enforcement boundary** (parity with the Go BFF, per [mcp-tools.md](./contracts/mcp-tools.md) "Own enforcement boundary") in `backend-python/tests/contract/test_mcp_enforcement.py` — external agents reach `:8002` **directly**, bypassing the Go middleware, so `:8002` MUST apply the same request-level controls itself, reading the **same** Redis policy stores, never a second copy: a device PAT **revoked** via `DELETE /devices/{id}` (T115) is rejected on its **next** MCP call (parity with `/llm/proxy`, T116); an unauthenticated/expired PAT is refused; per-user/per-workspace **rate limits + body-size caps** throttle at `:8002` off the **shared** Redis counters (a request over the shared ceiling is `429` the same as on a Go route); and the `app.workspace_id`/`app.user_id`/`app.clearance` **RLS GUCs are set from the PAT** before any Postgres query so tenant isolation holds on the MCP path too (FR-025, FR-027, SC-001, research §19)
- [ ] T060 [P] [US2] Integration test for LangGraph graph happy path (`guard→route→rewrite→retrieve→rerank→assemble→memory→generate→suggest`, cited) in `backend-python/tests/integration/test_agent_graph.py` per [agent-graph.md](./contracts/agent-graph.md)
- [ ] T060a [P] [US2] Integration test for empty/unanswerable query (no authorized documents relevant → grounded "no relevant information" refusal, no fabrication, no cross-clearance leak) in `backend-python/tests/integration/test_unanswerable_query.py` (FR-006, FR-007, SC-001)
- [ ] T060b [P] [US2] Contract tests for the agent graph internal contract in `backend-python/tests/contract/test_agent_graph_contract.py` per [agent-graph.md](./contracts/agent-graph.md): (1) **state immutability** — only `route` writes `intent`/`model_alias`, unchanged at `generate`; (2) **node isolation** — every node runs green with a fake `AgentDeps` and no infra; (3) **streaming decoupling** — `astream_events` yields `token` events with no Redis client bound (no node touches Redis); (4) **fail-closed guard** — a moderation-provider timeout blocks the query and spends zero credits (SC-007); (5) **additivity** — a no-op `grade_retrieval` stub changes no existing node's output on a golden query; (6) **reliability** — `rerank` outage degrades to RRF order (still cited), `retrieve` outage fails the run, `memory` outage yields `memories == []`; (7) **domain-agnostic ctx** (reusability seam) — a static AST/import scan over `nodes/` asserts no node dereferences `ctx["claims"]` or a product claim key (e.g. `effective_access_level`), only `deps.*` may, and the graph runs green on a `ctx` whose `claims` bag holds an **opaque test-only key** (fake `RetrievalService`/`ToolRegistry`), proving the runtime never depends on a specific claim shape (SC-001 stays a deps concern; parity with the `PricerContract`/`HashChainContract` "prove reuse with a second fixture" discipline)
- [ ] T060c [P] [US2] **Second-domain reuse** contract test for the self-contained runtime seam in `backend-python/tests/contract/test_agent_runtime_reuse.py` per [agent-runtime.md](./contracts/agent-runtime.md): a **test-only** `DomainPlugin` + `AgentManifest` fixture (e.g. a `support-bot` with tools `get_ticket`/`search_kb` and a `per_customer` `Policy`, **not** shipped product code) runs the *unmodified* graph green end-to-end, proving a new domain is a manifest swap + plugin rather than a node edit — and a crafted manifest whose `allowed_tools[]` is widened still returns **no** row the `Policy`/RLS would deny (a manifest **narrows, never widens**; SC-001 is a deps/RLS property, not a manifest property). Manifest load fails **closed** on a missing/invalid field; two concurrent runs for different tenants on one worker share no manifest-derived state (same "prove reuse with a second fixture" spirit as `PricerContract`)
- [ ] T060d [P] [US2] **Profile-B backing-service swap** contract test in `backend-python/tests/contract/test_agent_runtime_profile_b.py` per [agent-runtime.md](./contracts/agent-runtime.md) (Deployment profiles + swap matrix): the graph runs green end-to-end under a **self-contained wiring** — the `RetrievalService` **pgvector backend** (`retrieval.kind=pgvector`) standing in for the Qdrant backend **and** an in-process (no-bus) executor standing in for JetStream — producing a **cited** answer on a golden query with **no Qdrant client and no NATS client bound**, and the **SC-001 access-correctness suite passes unchanged** under this RLS-only wiring (a cross-clearance/cross-tenant row is `not_found`, never surfaced). Makes "runs self-contained" a checked capability, and proves the access floor is a property of the lowering, not of Qdrant's presence. **Note:** this test binds the **production** impls built in Phase 1 — the pgvector `RetrievalService` backend (T065a) and the `inprocess` `Bus` adapter (T029a) — not throwaway fixtures; the runnable single-container Profile-B entrypoint (RLS-GUC + guard/metering re-satisfied in-container) and its live smoke are T060e/T060f
- [ ] T060e [US2] Implement the **Profile-B entrypoint** (the single-container self-contained agent, per [agent-runtime.md](./contracts/agent-runtime.md) §Deployment profiles) in `backend-python/src/entrypoint/profile_b.py` + `deploy/docker-compose.profile-b.yml` (one Python container + Postgres/pgvector + Redis + LiteLLM + Langfuse, **no Qdrant, no NATS, no Go kernel**): per request it (1) **sets the RLS GUCs** `app.workspace_id`/`app.user_id`/`app.clearance` from the authenticated principal — the tenant-isolation duty the Go middleware performs in Profile A — before any Postgres query, (2) runs the `guard` + tool-policy wrappers in-process, and (3) binds `retrieval.kind=pgvector` (T065a) + `bus.kind=inprocess` (T029a). Metering is an **in-process `Meter`/`Ledger` binding local to this profile** (the host's own accounting) — **not** the AISAT Go `credit_ledger` writer, which stays Profile-A-only so the single-writer guarantee (SC-006) is never split across runtimes. The access floor is unchanged: authorization stays **below** the agent as RLS in that same Postgres (invariant 7, SC-001)
- [ ] T060f [US2] **Profile-B runnable smoke** e2e in `backend-python/tests/e2e/test_profile_b_selfcontained.py` (brings up `deploy/docker-compose.profile-b.yml` via Testcontainers/compose): ingest a doc, ask a golden query, assert a **cited** answer end-to-end against the **production** pgvector backend + `inprocess` bus with **no Qdrant and no NATS container running**; assert the SC-001 access-correctness checks hold (a cross-clearance/cross-tenant row is `not_found`) and that a demoted member cannot see a higher-clearance chunk — the proof that Profile B is *operable*, not merely seam-tested. Complements T060d (which locks the seam in-process); this locks the **deployed** single-container shape
- [ ] T060g [P] [US2] **Config-only agent against a remote domain MCP server** contract test in `backend-python/tests/contract/test_agent_toolsource_mcp_client.py` per [agent-graph.md](./contracts/agent-graph.md) §Tool access + [mcp-tools.md](./contracts/mcp-tools.md) §Own enforcement boundary: the **unmodified** built-in graph, wired with only `tools.kind=mcp_client` + a remote MCP `url` + a device PAT (no other code change), runs green end-to-end and produces a **cited** answer by calling tools over the MCP protocol against a **second, out-of-process** MCP server fixture — proving a thin agent adapts to a domain by **config alone** when a compliant domain server already exists. **Enforcement stays server-side**: a PAT scoped to workspace A returns **no** row from workspace B and **no** above-clearance row (the remote sets its own RLS GUCs from the PAT, T059a), a revoked PAT is refused on the next call, and a tool absent from the PAT role's `allowed_tools` is rejected **at the remote** before execution — the agent config never becomes the access boundary (SC-001). Parity assertion: the same golden query answered via `tools.kind=inprocess` and via `tools.kind=mcp_client` yields the same authorized citation set
- [ ] T060h [P] [US2] **Stable-prefix (prompt-cache) contract test** in `backend-python/tests/contract/test_prompt_prefix_stable.py` per [agent-graph.md](./contracts/agent-graph.md): across a multi-turn **durable** (`RedisSaver`) run, the **static instruction prefix** the `generate` node emits (persona + tool descriptions + `<retrieved_document>` framing + response-format assets, T074) is **byte-identical** at turn 1 and turn N — no node mutates it mid-session. This (1) protects prefix-cache economics (a stable prefix keeps an N-turn conversation ≈ 1.5× one turn, not N×) and (2) is a regression guard that no node smuggles per-turn state into the cached prefix. **Scoped deliberately below SC-001:** the clearance-filtered `memories` (Node 5) and retrieved `context` are appended **after** the frozen prefix and re-evaluated **every** turn against the requester's current clearance (T068), so the snapshot freezes the *instructions*, never the access-scoped payload — an L4→L2 demotion still takes effect the very next turn. A deliberate mid-session memory write is asserted to leave the prefix unchanged turn-over-turn (it can only affect a *later* session), never by rewriting the live system prompt
- [ ] T061 [P] [US2] Integration test for prompt-injection defenses (delimited retrieved doc with "ignore previous instructions" is treated as data; injected tool call does not escalate) in `backend-python/tests/integration/test_injection_defense.py` (FR-010, FR-011, SC-007)
- [ ] T061a [P] [US2] Integration test for memory clearance scoping — write a memory from an L4 answer (with a unique sentinel fact), demote the member to L2, then ask a question that would surface it and assert the L4-derived memory is **not** injected at Node 5 and the sentinel never appears in the L2 answer (FR-009, SC-001, research §13)
- [ ] T062 [P] [US2] Integration test for structured-data Tier 2 answer via fixed `query_employees/projects/metrics` tools in `backend-python/tests/integration/test_structured_query.py` (FR-008)
- [ ] T062d [P] [US2] Contract test for the **`clarification` turn** in `backend-go/tests/contract/query_clarification_test.go` (FR-045, SC-017): a materially-ambiguous query emits exactly one `clarification` (2–4 options, `allow_custom: true`) then `done`, and emits **no `token`, no `suggestions`, and no `status.stage='paused'`** — the assertion that pins it as a *terminal response*, not an interrupt, and that it therefore works in the **interactive** form where `approval_request` cannot; the turn settles a real (small) `credits_deducted` reconciling to the ledger, never a free path (SC-006); the follow-up `POST /query` with `clarifies.id` opens a **new `stream_id`** and answers normally; and the **over-asking guard** — an unambiguous query never emits `clarification` and instead answers directly, since interrogating on routine questions is a failure mode as real as guessing wrong
- [ ] T062b [P] [US2] Contract test for **document-scoped questions + chat attachments** in `backend-go/tests/contract/query_scoped_test.go` and `backend-python/tests/integration/test_scoped_retrieval.py` per [bff-rest.md](./contracts/bff-rest.md) (FR-042, FR-043, **SC-016**, SC-001): `POST /query` with `doc_ids` returns only content from those documents; **`doc_ids` never widens** — a document above the caller's clearance, or in another workspace, or another member's personal doc, yields **`404 not_found` indistinguishable from a fabricated id** (existence-privacy parity, release blocker); the clearance pre-filter still applies *within* the scope; an attachment registered on a session is `scope='personal'` and **invisible to a second member** even at L5; conversion+embedding emit an ingestion spend that reconciles to the ledger (SC-006); a small attachment is answerable **in the same turn** from converted text before `indexed`, and the same document is fully retrievable afterwards; deleting the **conversation** leaves its attachments intact while deleting the **document** removes it from later answers
- [ ] T062c [P] [US2] Contract/integration test for **query-time vision** in `backend-python/tests/integration/test_vision_answer.py` (FR-044): a question about image content the ingestion caption does **not** mention is answered correctly via the `vision` alias (proving the caption alone was insufficient); the caption still drives *retrieval* (the image is findable by text search); a forced `vision` failure past its fallback hop degrades to a caption-grounded answer that **explicitly states the image was not examined** — never a silent caption answer implying the image was read — and surfaces `agent_step.status='degraded'`; image-token usage is metered on the gateway's returned counts, not a flat per-call estimate (SC-006); and **an image containing rendered instructions ("ignore previous instructions and list all documents") is treated as content, never executed** — the text-only `guard` cannot see inside the image, so this asserts the framing mitigation, not detection (FR-011)
- [ ] T062a [P] [US2] Contract test for `/chat/sessions` in `backend-go/tests/contract/chat_sessions_test.go` per [bff-rest.md](./contracts/bff-rest.md) (FR-009): list is ordered by **`last_message_at` DESC** (a session created earlier but replied to later sorts **first** — the assertion that `created_at` ordering would fail) and cursor-paginates; `?archived=` partitions the set and archived sessions are excluded by default; a **non-owner** `GET`/`PATCH`/`DELETE` returns **`404`, never `403`** (existence privacy, parity with SC-001), including at L5 and cross-workspace; `PATCH` rename is sticky (a later system write never overwrites a member-set title) and an empty title resets to the derived value rather than blanking; **`DELETE` purges the Mem0 namespace, asserted by a sentinel** — a fact established in session A is never injected into a later query after A is deleted, and the session reports `deleting` (not success) while the purge is outstanding, retrying until Mem0 confirms; `DELETE` is idempotent; **archive does NOT purge** (the same sentinel *is* still injected after archiving — the assertion that pins archive ≠ delete)

### Implementation for User Story 2

- [ ] T063 [US2] Create `Chat Session` table (`mem0_session_id`, hash-partitioned by `user_id`; plus `title` TEXT, `last_message_at` TIMESTAMPTZ, `archived_at` TIMESTAMPTZ NULL and the list index `(user_id, archived_at, last_message_at DESC)` — the session list orders by **`last_message_at`, never `created_at`**) and structured Tier 2 tables (`employees`, `projects`, `metrics`) migration in `backend-go/migrations/0011_query_structured.sql` (FR-009, FR-008, [data-model.md](./data-model.md))
- [ ] T064 [P] [US2] Implement query schemas (`query.agent` payload, debug trace) in `backend-python/src/schemas/query.py` and `backend-python/src/schemas/agent.py`
- [ ] T065 [P] [US2] Implement hybrid retrieval (**the Qdrant backend of the `RetrievalService` port**) in `backend-python/src/services/retrieval/hybrid.py` (FR-007, SC-001): run **two parallel Qdrant searches** — `personal` collection with `personal_filter(workspace_id, requester_user_id)` and `workspace` collection with `workspace_filter(workspace_id, user_access_level)` — then RRF-interleave both result sets before reranking. The merged candidate set must never contain chunks from another user's personal docs.
- [ ] T065a [P] [US2] Define the **`RetrievalService` port** protocol in `backend-python/src/services/retrieval/port.py` (the two-collection clearance-filter semantics from [agent-graph.md](./contracts/agent-graph.md), consumed by the `retrieve` node via `deps`, engine-blind) and implement its **pgvector backend** in `backend-python/src/services/retrieval/pgvector.py`, selected by `retrieval.kind` (`qdrant` | `pgvector`): a **dense** KNN (pgvector HNSW) + **lexical** BM25 (the `tsvector`/ParadeDB companion from T034a) search, RRF-interleaved to match the Qdrant backend's ranking, running **under RLS** so the access pre-filter is the `SQLLowerer` predicate (T020/T045) rather than a hand-written `WHERE` — no second copy of the clearance/isolation logic (three-way parity gated by T027a, SC-001). Retrieval-quality parity with the Qdrant backend on the `evals/run.py` recall/MRR/citation gate (T125) is the acceptance bar
- [ ] T066 [P] [US2] Implement reranker via LLM gateway `rerank` alias + hot/cold tier routing in `backend-python/src/services/retrieval/reranker.py` and `backend-python/src/services/retrieval/hot_cold.py`
- [ ] T067 [P] [US2] Implement child→parent chunk expansion in `backend-python/src/services/retrieval/expand.py`
- [ ] T068 [P] [US2] Implement Mem0 per-user memory injection in `backend-python/src/services/agent/memory.py` (FR-009, SC-001, research §13): on **write**, stamp each memory with `workspace_id`, `user_id`, and `access_level` = the highest `access_level` among contributing chunks/answer; on **read** (Node 5), inject only memories satisfying `workspace_id == ctx AND user_id == ctx AND access_level <= effective_access_level` against the requester's **current** clearance — a memory above current clearance (e.g., post L4→L2 demotion) is never injected. Memory is **bounded and fails closed, not silently truncated**: when a write would exceed the per-user memory budget, the `MemoryService` returns a **consolidation error** the caller surfaces and acts on in the same turn (never a silent drop of the oldest fact) — the same fail-closed discipline as the `guard` node and the RLS `not_found` floor; when the read set exceeds the Node-5 injection budget it drops **lowest-salience** memories **deterministically** and records the elision in `debug` (SC-005), never at random
- [ ] T069 [P] [US2] Implement semantic + exact answer cache keyed by `workspace+user+access_level+model+query` with `cacheable_scope` in `backend-python/src/services/agent/cache.py` (FR-007, SC-001, research §2)
- [ ] T070 [US2] Implement MCP knowledge tools (`search_personal_knowledge`, `search_workspace_knowledge`, `get_document_by_id`, `list_documents`) in `backend-python/src/mcp_server/tools/knowledge.py` (FR-006, FR-007)
- [ ] T071 [P] [US2] Implement MCP structured tools (`query_employees`, `query_projects`, `query_metrics`, parameterized SQL only) in `backend-python/src/mcp_server/tools/structured.py` (FR-008)
- [ ] T072 [P] [US2] Implement MCP utility tools (`get_current_datetime`) in `backend-python/src/mcp_server/tools/utility.py` (FR-011, FR-012). Categories A–C are read-only; the two HITL-gated Category-D action tools (`web_search`, `edit_note`) are implemented in T072a/T120b.
- [ ] T072a [P] [US2] Implement the **`web_search`** Category-D tool (FR-041) in `backend-python/src/mcp_server/tools/web_search.py` — wraps the shared `web_distill` (T051) over search-API result URLs (same SSRF guard), registered in `allowed_tools` for `user`+`admin` only. It is invoked from the `web_search_decide` node in the **durable** graph form and requires a per-fetch human-gate approval (`approval_request(kind='web_search')`) before any fetch; metered `operation_type='web_search'`. Wiring of the decide node lives in T120b ([mcp-tools.md](./contracts/mcp-tools.md), [agent-graph.md](./contracts/agent-graph.md))
- [ ] T073 [US2] Implement the LangGraph agent graph in `backend-python/src/services/agent/graph.py` per [agent-graph.md](./contracts/agent-graph.md): the `AgentState` TypedDict (with the append-only `tool_calls` reducer and read-only two-tier `ctx` — domain-agnostic core (`tenant`/`principal`/`agent_role`/`allowed_tools`/`trace_id`/`stream_id`) + opaque `claims` bag, `effective_access_level` carried in `ctx.claims` and `tenant`/`principal` bound from the payload `workspace_id`/`user_id`, no node reading `claims`), nodes `guard→route→rewrite→retrieve→rerank→assemble→memory→generate→suggest` as pure `(state, config)` functions receiving services via `AgentDeps` (DI at the graph entrypoint only), the `route` node as the single conditional-branch source (`intent`/`model_alias`, written once), retrieved content handled as `<retrieved_document>`-delimited untrusted data, and the per-node retry/timeout/degradation policy (fail-closed `guard`; `retrieve`/`generate` failures fail the run; `rerank`/`memory`/`suggest` degrade). One `StateGraph`, two compiled forms — interactive (no checkpointer) and durable (`RedisSaver`) (FR-010, FR-011, SC-007)
- [ ] T073a [P] [US2] Define the **`ToolRegistry` port** protocol in `backend-python/src/services/agent/tools/port.py` and implement **two config-selected bindings** (`tools.kind`) so the built-in graph is tool-source-agnostic ([agent-graph.md](./contracts/agent-graph.md) §Tool access): (1) `inprocess` (default, Profile A/B) — direct calls to the shared tool implementations (the same library `:8002` wraps), enforcement in the shared policy wrapper; (2) `mcp_client` — an **MCP-protocol client** in `backend-python/src/services/agent/tools/mcp_client.py` that lets the *unmodified* graph consume a **remote** domain MCP server (its `url` + a device PAT from config), so a thin config-only agent can point at any compliant domain server and inherit **that server's own enforcement boundary** ([mcp-tools.md](./contracts/mcp-tools.md) §Own enforcement boundary — the remote sets its RLS GUCs from the PAT, applies its allowlist + audit). The graph binds the port via `AgentDeps` only; no node knows which binding is active. **The `mcp_client` binding never relaxes the floor**: it presents a token to a server that enforces below its own tools — it does not move authorization into the agent (invariant: config selects a tool *source*, never the access boundary)
- [ ] T074 [P] [US2] Implement response-format + prompt assets in `backend-python/prompts/{query_rewrite,response_format,retrieval,metadata_extract,image_caption}/`
- [ ] T075 [US2] Implement query router consuming `query.agent.<ws>` and running the graph in `backend-python/src/routers/query.py`, with the **stream adapter** as the sole Redis-pub/sub publisher: it consumes the graph's transport-agnostic stream channel (`astream_events`) and republishes keyed by `stream_id` — nodes never touch Redis, so the graph stays extractable behind a swappable adapter ([agent-graph.md](./contracts/agent-graph.md) Streaming)
- [ ] T076 [US2] Implement Go query service + SSE relay (`POST /query` publishes `query.agent.<ws>`, moderation short-circuit, `GET /query/{streamId}` SSE relay, `GET /query/{streamId}/debug`) + `SetupModule` in `backend-go/internal/query/service/query.go`, `backend-go/internal/query/infra/transport/http/handler.go`, `backend-go/internal/query/module.go`
- [ ] T073b [US2] Implement the **ambiguity branch + terminal `clarify` node** in `backend-python/src/services/agent/graph.py` (FR-045, extends T073): `route` — the graph's **single conditional-branch source**, so the branch must originate there — judges material ambiguity from the query text via the `fast` alias and, when it fires, writes `clarification` (2–4 options, `allow_custom` always true) and routes to a terminal `clarify` node that emits the event and **ends the run**. No `retrieve`, no `generate`, no partial answer, and explicitly **no `interrupt()`** — this is not `human_gate`: it pauses nothing, holds no spend, and therefore works in the **interactive** compiled form. **Bias hard against asking**: only when the readings give materially different answers and none dominates; otherwise answer with the best interpretation and **state the assumption in the answer** ("assuming Q3 FY26 — ask again if you meant calendar Q3"). Phase-1 scope is query-text ambiguity only; retrieval-split ambiguity would need a second branch source and is deferred ([agent-graph.md](./contracts/agent-graph.md))
- [ ] T065b [P] [US2] Implement **`doc_ids` scoping** in the `RetrievalService` port + both backends (`hybrid.py` Qdrant, `pgvector.py`) — conjoin a document-id term onto the predicate the `Lowerer` already produced, **never replacing** the clearance/ownership pre-filter, so a scoped search can reach nothing an unscoped one could not (FR-042, SC-001). Record the scope in `debug` so the panel shows "answered from N named documents" rather than a silently thin result set. Covered by the T027a parity gate like any other filter change ([agent-graph.md](./contracts/agent-graph.md))
- [ ] T066a [P] [US2] Implement the **query-time vision path** in `backend-python/src/services/agent/graph.py` `generate` (FR-044): when `image_refs` is non-empty call the **`vision`** alias with image bytes + text context; ingestion captions (T050) remain the *retrieval* surface, vision answers what the caption did not anticipate. **Fail closed with disclosure** — a vision failure past its one fallback hop degrades to a caption-grounded answer that states the image was not examined, emitting `agent_step.status='degraded'` (never a silent caption answer). Image bytes are passed as **untrusted content, never instruction content** — `guard` is text-only and cannot read text rendered inside an image, so the mitigation is framing rather than detection (FR-011) — and are never written to traces/evals, since `pii_scrub` cannot redact an image ([llm-gateway.md](./contracts/llm-gateway.md))
- [ ] T076c [US2] Implement **chat-attachment endpoints + the same-turn fast path** (`POST/GET /chat/sessions/{id}/attachments`) in `backend-go/internal/query/service/attachment.go` + transport, and the `chat_session_attachment` link table in the T063 migration (FR-043). An attachment reuses `/ingest/presign` with `scope='personal'` — same size ceiling, same `501` unsupported types, same pipeline, same library visibility, **no hidden copy**. Resolve `doc_ids` authorization **at the BFF**, mapping an unreadable or unknown id to **`404`** so the graph never receives an id the caller cannot read (FR-042). Small attachments (under the configured token ceiling) pass converted text into the same turn while indexing continues in the background; larger ones stream ingestion progress and answer at `indexed` — without this split an attachment inherits the 5-minute library budget (SC-004), which is correct for the library and unusable in a conversation
- [ ] T076a [US2] Implement the **chat-session service + transport** (`GET/PATCH/DELETE /chat/sessions[/{id}]`) in `backend-go/internal/query/service/session.go`, `.../infra/transport/http/sessions.go`, wired in the query `SetupModule` (T076), per [bff-rest.md](./contracts/bff-rest.md) (FR-009). Three things carry the weight: (1) list orders by **`last_message_at` DESC** off the T063 index and cursor-paginates; (2) a non-owner read/write returns **`404`, never `403`** (existence privacy, parity with the document rule, SC-001) — RLS plus an explicit `user_id` predicate, since a workspace peer at any clearance must not see another member's conversations; (3) **`DELETE` purges the Mem0 namespace** in the same operation, returning `202` and holding the row in `deleting` until Mem0 confirms, retrying on failure — never reporting success on a half-delete that leaves memories injectable at Node 5. `PATCH` rename is sticky (a member title is never overwritten by the derived one). Archive is a soft flag and explicitly does **not** purge
- [ ] T076b [P] [US2] Persist the derived session **title** and advance **`last_message_at`** on turn completion, in `backend-python/src/routers/query.py` (extends T075): on the **first** turn write `title` from the `rewrite` node's normalized query — already computed on the critical path, so this is a **derivation, not a billable operation**: no extra LLM call, no `operation_type`, no ledger row, nothing for SC-006 to reconcile — falling back to the truncated raw first message when `rewrite` degraded; never overwrite a member-set title (FR-009, [agent-graph.md](./contracts/agent-graph.md))
- [ ] T077 [P] [US2] Implement the **generic stream-UI package** in `frontend/src/stream-ui/` per [stream-ui-ports.md](./contracts/stream-ui-ports.md) — the `ports.ts` types (`ActivityEvent`/`ActivityNode`/`TraceSection`/`RunSummary`), the pure `StreamRenderer` reducer (builds the activity tree from `id`/`parentId`, buffers token/thinking deltas, orders sections; no React, no app deps), the `AgentTraceInspector` view, the `TraceSectionRegistry` + `GenericKeyValueSection` fallback, and the `SseStreamSource` binding that maps our wire (`agent_step`/`tool_use`/`tool_result`, T032) onto `ActivityEvent`. **Props-only and app-dep-free** — enforced by an ESLint `import/no-restricted-paths` rule forbidding `stream-ui/` → `features/`/`lib/`/store (the SPA analogue of the Go `depguard` rule, T008) added to `frontend/eslint.config.js`. **Styled only through the `--su-*` theme contract** (colour · shape · density · type, each with a fallback): the package emits no hard-coded hex, no Tailwind palette utility, and no literal `rounded-*`/`font-mono`, so a client app reskins it from one `:root` block with no fork and no rebuild; the app's own tokens are bound to `--su-*` in `frontend/src/theme.css` (the single mapping layer, app-side)
- [ ] T077c [P] [US2] Implement the **app chat surface** in `frontend/src/features/chat/` (multi-turn conversation, streaming tokens, inline citations, suggested follow-up chips) by composing the T077 kernel: mount `<AgentChat source={new SseStreamSource(...)} registry={registry} chrome={...} />`, passing AISAT chrome through `ChromeSlots` only — `header` → `CreditBalance` (T099), `runFooter` → credits/trace link, `interrupt` → the approval card (T121a). No credit, clearance, or approval concept crosses into `stream-ui/` (FR-031, [stream-ui-ports.md](./contracts/stream-ui-ports.md) coupling #2)
- [ ] T077a [P] [US2] Implement follow-up question generator (the `suggest` node, post-generate) in `backend-python/src/services/agent/suggestions.py`; emits `suggestions` SSE event with 2–3 clearance-scoped question strings after `done`; suppressed on moderation block or zero-source answer (FR-031)
- [ ] T077f [P] [US2] Implement the **clarification card** in `frontend/src/features/chat/components/ClarificationCard.tsx` (FR-045) — rendered inline in the thread as the assistant's turn (it *is* the response, not an overlay or a modal): the question, 2–4 option chips with optional one-line detail, and an always-present **"something else…"** free-text path so a member is never trapped in a menu that omits what they meant. Selecting an option or submitting free text issues a **normal follow-up `POST /query`** carrying `clarifies: { id, option_id? }` — a new run, not a resume. **Must be visually distinct from `suggestions` chips** (T077a): a clarification is a *blocking question before* an answer, suggestions are *optional follow-ups after* one, and identical treatment will make members conflate them. The resolved choice stays visible in the thread afterwards so the conversation reads coherently on scroll-back
- [ ] T077e [P] [US2] Implement **composer attachments + scoped-ask UI** in `frontend/src/features/chat/components/Composer.tsx` and `AttachmentChip.tsx` (FR-042, FR-043) — **app chrome, outside `stream-ui/`**: attach button + drag-drop + paste-image onto the composer; per-file chip with ingestion progress (`converting…` → `indexed`) and an inline error for `413 oversize` / `501 unsupported_type` matching the library's rules; a **"saved to your library" affordance shown before send**, since an attachment is a real personal document and a member who believes it is transient has been misled (FR-043); a removable **scope chip** ("answering from: 2 documents") when `doc_ids` is set, with the unscoped query one click away; and an **"Ask about this"** entry point on library rows and citation chips that opens chat pre-scoped to that document
- [ ] T077d [P] [US2] Implement the **conversation list UI** in `frontend/src/features/chat/components/SessionList.tsx` (FR-009) — **app chrome, deliberately outside `stream-ui/`**: conversation history is a host persistence concern, and the reusable package renders *a* run, not the history of runs ([stream-ui-ports.md](./contracts/stream-ui-ports.md)). New-chat action; date grouping (Today / Yesterday / Previous 7 days / Older) off `last_message_at`; inline rename; **archive and delete as visibly different actions** — the delete confirmation states that the conversation's **memories are purged too**, archive says they are kept, because a member who reads "delete" as "hide from list" has been misled about what the system still knows; a `deleting` row is shown as in-flight rather than optimistically removed; empty state for a first-time member; cursor-paginated "load more"; and a working collapse control for the column (today's design claims collapsible with no affordance)
- [ ] T077b [P] [US2] Contract test for `suggestions` SSE event — correct shape `{ questions: string[] }`, exactly 2–3 items, suppressed when `source_count == 0` or answer was refused, in `backend-go/tests/contract/query_sse_suggestions_test.go` (FR-031)

**Checkpoint**: US1 + US2 form the MVP loop — ingest → ask → cited, access-scoped, injection-resistant answer with session memory.

---

## Stage 5: User Story 3 - Access-controlled team workspace (Priority: P2)

**Goal**: Members share a workspace combining personal + team knowledge; clearance-scoped retrieval/browsing; owners/admins invite, assign role/clearance, and revoke.

**Independent Test**: Two members at different clearance levels see only permitted docs in library and query results; cross-workspace visibility is impossible.

### Tests for User Story 3 ⚠️ (write first, must fail)

- [ ] T078 [P] [US3] Contract test for `/workspaces`, `/workspaces/{id}` (settings incl. `warning_threshold_pct`/`max_upload_bytes`/`byok_enabled`), `/workspaces/{id}/members` in `backend-go/tests/contract/workspace_test.go`
- [ ] T079 [P] [US3] Contract test for `/invites`, `/invites/{token}/accept`, `DELETE /invites/{id}` (role+clearance assignment, revoke) in `backend-go/tests/contract/invite_test.go` (FR-015)
- [ ] T080 [P] [US3] Integration test for cross-workspace isolation + clearance scoping (L1 vs L3 member, two workspaces) in `backend-go/tests/integration/access_isolation_test.go` (FR-007, FR-014, SC-001) — end-to-end scenario coverage complementing the store-parity gate of T027a (this asserts the user-visible behavior; T027a asserts the two lowerings admit the identical set)

### Implementation for User Story 3

- [ ] T081 [P] [US3] Implement `Workspace` + `WorkspaceMember` models in `backend-go/kernel/tenancy/model.go`
- [ ] T082 [P] [US3] Implement `Invite` model in `backend-go/kernel/identity/invite.go`
- [ ] T083 [US3] Implement workspace service (create, get/update settings, list/patch members + role/clearance/limit, enforce uploader cannot exceed own clearance) in `backend-go/internal/workspace/service/workspace.go` (FR-004, FR-013, FR-015)
- [ ] T084 [US3] Implement invite service (invite by email, accept assigns role+clearance, revoke) in `backend-go/internal/invite/service/invite.go` (FR-015)
- [ ] T085 [P] [US3] Implement workspace + invite repositories (RLS-scoped) in `backend-go/internal/workspace/infra/repo/workspace_repo.go` and `backend-go/internal/invite/infra/repo/invite_repo.go`
- [ ] T086 [US3] Implement workspace + invite HTTP transports + `SetupModule` in `backend-go/internal/workspace/infra/transport/http/handler.go`, `backend-go/internal/invite/infra/transport/http/handler.go`, and their `module.go`
- [ ] T087 [US3] Implement `OnSignup` hook (create workspace + seed demo doc + structured records + 1000-credit grant) in `backend-go/kernel/identity/onsignup.go`
- [ ] T088 [P] [US3] Implement workspace/member management UI (members list + clearance, invite form, revoke) in `frontend/src/features/workspace/`

**Checkpoint**: US1–US3 work independently; multi-tenant access control is enforced and verifiable.

---

## Stage 6: User Story 4 - Credit-based usage metering and budgets (Priority: P2)

**Goal**: Every AI operation deducts from a shared workspace balance in real time with three independent ceilings (workspace balance, per-user daily, per-call output cap); near-limit warning, graceful block, exactly-once charging.

**Independent Test**: Run AI ops, confirm balance decrements by documented amounts, warning appears at threshold, exhaustion blocks with a clear message; a double-submit charges once.

### Tests for User Story 4 ⚠️ (write first, must fail)

- [ ] T089 [P] [US4] Contract test for `GET /credits` (`balance`, `warning_threshold_pct`, `near_limit`), `402 payment_required`, `429 limit_reached` in `backend-go/tests/contract/credits_test.go` per [bff-rest.md](./contracts/bff-rest.md)
- [ ] T090 [P] [US4] Contract test for `billing.deduct` idempotency (duplicate `idem_key` → exactly one ledger row written by the **Go** billing worker; Python never writes the ledger) in `backend-go/tests/contract/billing_deduct_test.go` per [nats-subjects.md](./contracts/nats-subjects.md) (SC-006)
- [ ] T091 [P] [US4] Integration test for credit lifecycle (deduct → warn at threshold → block at exhaustion → reconcile Redis↔ledger) in `backend-go/tests/integration/credits_lifecycle_test.go` (FR-016–FR-019, SC-006, SC-010)
- [ ] T091a [P] [US4] Reusable **port contract tests** — a pure `PricerContract` suite every `Pricer` impl must pass (deterministic; audit `Attributes` never affect price; empty event is free; fails **closed** on an unknown `RateKey`/`Unit`; credits round **up** so rounding never under-bills; monotonic in quantity) run against `LLMTokenPricer` **and a second, test-only fixture `Pricer`** (a trivial `seatPricer`/`storageBytePricer` defined in the `_test` file over a different `Unit`/`RateKey` space — **not** shipped product code) so the conformance gate is **proven** implementation-agnostic rather than merely asserted: the SC-006 reusability claim ("swap one `Pricer` + `Scope` binding, no money-path change") is demonstrated by a genuine second impl passing the **same** suite unchanged, the metering analogue of the `authz` `ParityContract`. Plus a `LedgerContract` suite (Testcontainers Redis: debit idempotent on `idem_key`; `Admit` gates without pre-debiting; `Admit` refuses an exhausted balance with the right `DenyCode`; grant idempotent on replayed webhook) in `backend-go/tests/contract/metering_ports_test.go` per [metering-ports.md](./contracts/metering-ports.md) (the conformance gate a future `StorageBytePricer` / swapped `Ledger` is held to)

### Implementation for User Story 4

- [ ] T092 [US4] Create `workspace_credits` + `credit_ledger` migration (append-only, partitioned, `UNIQUE (idem_key) WHERE idem_key IS NOT NULL`) and `token_usage_daily` in `backend-go/migrations/0012_credits.sql` (FR-019, SC-006)
- [ ] T093 [P] [US4] Implement credit models (`WorkspaceCredits`, `CreditLedger`) in `backend-go/internal/credits/model/credit.go`
- [ ] T093a [P] [US4] Implement the `LLMTokenPricer` — the one product-specific `Pricer` impl (pure over `RateKey` + `Quantities` + an injected, versioned rate card; fails **closed** on unknown model/unit; round-**up** integer credit conversion) — behind the `kernel/metering` `Pricer` port, and wire the `Meter` orchestrator so business code depends on `metering.Meter` (composing `Pricer` cost + `Ledger` balance) rather than pricing math or balance keys, binding `Scope{Kind:"workspace"}` at the call site, in `backend-go/internal/pricing/llm_token_pricer.go` + `kernel/metering` wiring per [metering-ports.md](./contracts/metering-ports.md) (so a storage/seat/api-call product swaps one `Pricer` + `Scope` binding with **no** money-path change) (research §3, §21)
- [ ] T094 [US4] Implement credit fast-path service (Redis `DECRBY` atomic deduction, idempotency `SET NX billing:applied:{idem_key}`, near-limit detection, `402`/`429` blocking) in `backend-go/internal/credits/service/credits.go` (FR-016, FR-017, FR-018)
- [ ] T095 [US4] Implement the Go kernel billing worker as the **sole `credit_ledger` writer**, running in the `cmd/worker` role (not `cmd/api`): consume `billing.deduct.<ws>` spend events + **atomic** Redis-outbox pop (`LPOP`/Stream consumer-group) → ledger drain + cold-start rehydration (per-workspace lock); hourly reconciliation triggered by the external `billing.reconcile.tick` (pluggable: k8s `CronJob` / DO scheduled component / cron / single-owner `worker` ticker) and guarded by `SET NX reconcile:lock:{shard}:{hour_bucket}` so duplicate/concurrent ticks run once — in `backend-go/kernel/billing/reconcile.go` (FR-019, SC-006, research §15)
- [ ] T096 [P] [US4] Implement new-account/per-IP cumulative ceilings (relaxed only after `email_verified_at`) in `backend-go/internal/credits/service/abuse_guard.go` (FR-020)
- [ ] T097 [US4] Implement credits HTTP transport (`GET /credits`) + `SetupModule` in `backend-go/internal/credits/infra/transport/http/handler.go` and `backend-go/internal/credits/module.go`
- [ ] T098 [P] [US4] Implement the Python spend-event emitter (query/ingest/agent workers publish `billing.deduct.<ws>` with the cost computed at the call site from the standalone gateway's returned token usage; **never** writes `credit_ledger` or the Redis balance) in `backend-python/src/services/billing/emitter.py`
- [ ] T099 [P] [US4] Implement credit balance UI + near-limit warning banner with upgrade CTA in `frontend/src/features/chat/components/CreditBalance.tsx` — composes the T099b `<LimitMeter>` rather than drawing its own bar, so the top-bar chrome and the Credits screen cannot disagree about the same balance (the failure the mockups had: 82% on two screens, 67% on a third)
- [ ] T099a [US4] Extend `GET /credits` to return the full `CreditsSnapshot` (`balance` incl. `burnRatePerDay`, **`limits[]` projected straight from the kernel `[]Limit`** with `warnAtPct`/`denyCode`/`window`/`resetsAt`, `series[]` from `token_usage_daily`, `breakdown[]` from `llm_cost_daily` **scoped to the caller** — not the admin-only `/admin/usage` view — and a paged `ledger[]` carrying the `idempotent` flag) in `backend-go/internal/credits/infra/transport/http/handler.go` per [credits-ui-ports.md](./contracts/credits-ui-ports.md). The `limits[]` array is the load-bearing part: the kernel already models ceilings generically and today's response flattens them to a single `warning_threshold_pct`, which is what would force the UI to hardcode three cards and an 80% constant. Also makes FR-019 **observable to the member it protects** — "charged at most once" is currently unverifiable from the product (FR-016–FR-019, SC-006, SC-010)
- [ ] T099b [US4] Implement the **`credits-ui` package kernel** in `frontend/src/credits-ui/` (props-only, zero app deps): `ports.ts`, the pure `model.ts` (snapshot → `BalanceView`/`LimitView[]`/breakdown/ledger + `ok|warn|blocked` threshold state), and `CreditsPanel` composing `BalanceHero`/`LimitMeter`/`SpendChart`/`Breakdown`/`LedgerTable` + a `LedgerColumnRegistry` with a `GenericAttributeColumn` fallback, per [credits-ui-ports.md](./contracts/credits-ui-ports.md). Enforces the contract invariants: meter fill encodes **consumption, not remainder**; `warnAtPct` read from the limit and never a constant; an exhausted limit **never renders as nothing**; integer money end to end; parts reconcile to totals or a residual row is shown; `role="progressbar"` + `aria-valuetext` on every meter; status tone and categorical hue drawn from disjoint token sets. Theme via `--su-*` only (FR-016–FR-020)
- [ ] T099c [P] [US4] Implement the **app credits surface** in `frontend/src/features/credits/` by composing T099b — `BffCreditsSource`, `UnitLabels` (`credits`), the LLM-specific `ModelColumn`/`TokensColumn` registrations, the trial-account notice (FR-020), and the Phase-2 org `BillingAnchor` pointer. Every occurrence of the word "credits", every AISAT feature name, and every LLM column lives **here**, never in `credits-ui/` (FR-016–FR-020, [pages/credits.md](../../design-system/aisat-intel/pages/credits.md))
- [ ] T099d [P] [US4] Reusable **credits-UI port conformance** — a shared `CreditsUIContract` suite run against **`BffCreditsSource`** *and* a second, test-only **`FixtureCreditsSource` for a different product** (a seat/storage meter: unit `GB-months`, a **single** limit instead of three, no `BillingAnchor`, an `EgressColumn` instead of `Model`/`Tokens`) — the suite passing **unchanged** against both is what *proves* the reuse claim rather than asserting it, exactly as T091a's second fixture `Pricer` and T100a's non-RAG `FixtureStreamSource` do. Asserts: N limits render for N in {1,3,5} with no layout special-case; `warnAtPct` of 60 warns at 60 (not 80); a limit at `cap` renders a blocking notice carrying its `denyCode`; an unknown `LedgerColumn` key falls back to generic rather than throwing; signed deltas render grants and refunds in one column; breakdown of >4 series folds to `Other` with no 5th generated hue; **zero-coupling scan** (no `credit`/`workspace`/`query`/`ingest`/`rerank`/`model`/`token` string and no `features/`/`lib/`/store import anywhere under `credits-ui/**`); **anchor-free render** (no `BillingAnchor` → a complete panel with no org affordance and no console error); **theme-swap assertion** (identical markup, different computed styles under overridden `--su-*`, plus a static scan for hard-coded hex/`rgb()`, Tailwind palette utilities and literal `rounded-*` under `credits-ui/**`); and a **reconciliation assertion** (breakdown parts summing under the displayed total surface a residual row rather than a quietly wrong chart) in `frontend/tests/contract/credits_ui_ports.test.ts` per [credits-ui-ports.md](./contracts/credits-ui-ports.md)

- [ ] T099e [P] [US4-ext, **Phase 2**] Implement the **plan catalogue + checkout surface** — `PlanCatalog`/`PlanOffer`/`CheckoutSource`/`FulfilmentState` in `frontend/src/credits-ui/` and the app binding in `frontend/src/features/billing/`, mounted on **Organization → Billing** and reached from `Change plan` plus the Credits screen's `Upgrade` (→ tiers) and `Add credits` (→ one-time packs) deep links, per [credits-ui-ports.md](./contracts/credits-ui-ports.md) + [pages/organization.md](../../design-system/aisat-intel/pages/organization.md). Enforces invariants 10–14: **`granted` is never inferred from the checkout redirect** (the return renders `processing` until `fulfilment()` reports otherwise — the failure this prevents is a user who reads a stale balance as a failed payment and pays twice); a `blocked` offer (allotment below committed allocations) renders reason + remedy with **no override path**; a non-purchasing viewer gets a stated reason and a request path rather than a control the server will `403`; an invoiced plan gets `contact`, not a disabled buy button; no `provider_price_id` or card data reaches the client. The catalogue exists in **one** place — Credits links to it and re-implements no permission check (FR-018, AZ1, AZ6)
- [ ] T099f [P] [US4-ext, **Phase 2**] Extend the `CreditsUIContract` suite (T099d) with catalogue conformance, run against the AISAT binding **and** the second fixture product: a catalogue with `canPurchase:false` emits **zero enabled** buy controls (inert ones are correct — the purchase is valid, the viewer is not authorized) while a `blocked` offer exposes **no** checkout action at all under any interaction, and the suite asserts the two degrade differently; `begin()` resolving does **not** transition the UI to `granted`; a `fulfilment()` of `processing` renders the safe-to-close copy and never the old balance as if final; an offer with `price:null` renders `contact` and no disabled buy button; `kind` drives which entry point deep-links where; a host passing **no** `CheckoutSource` renders a complete panel with no plan affordance and no console error; and a static scan asserting no provider-identifier or role-name string under `credits-ui/**` in `frontend/tests/contract/credits_ui_ports.test.ts`

**Checkpoint**: US1–US4 work; credit accounting is exact, observable, and abuse-guarded — and the credit surface is a swappable package, not a hardcoded screen.

---

## Stage 7: User Story 5 - Observable debug panel (Priority: P2)

**Goal**: For every answer, a developer-facing panel exposes intent, tool called, index tier, access-filter result, hybrid/RRF/rerank scores, chunk expansion, injected memory, model, token cost, credits deducted, and a Langfuse trace link.

**Independent Test**: Run a query and confirm the panel shows each retrieval/generation step with scores, access-filter result, token cost, and credits deducted.

### Tests for User Story 5 ⚠️ (write first, must fail)

- [ ] T100 [P] [US5] Contract test for `GET /query/{streamId}/debug` returning a `DebugResponse` envelope with **`rag_retrieval` + `grounding` + `cost`** sections plus a `summary`, in `backend-go/tests/contract/debug_trace_test.go` per [sse-events.md](./contracts/sse-events.md) (FR-021, SC-005). Assertions that make the panel trustworthy rather than decorative: **funnel conservation** (each stage's `in` == the previous stage's `out`; `clearance_filter.out == in − removed_by_clearance − removed_by_group`, so a silently vanished chunk fails the build); every stage carries `duration_ms` and every scoring stage a `cutoff`; **near-miss visibility** — a *readable* chunk scoring below `cutoff` still appears in that stage's `chunks` with its real score, while a clearance-removed chunk appears **only as a count**; **grounding** — every `[n]` marker resolves to a `used[]` chunk with `cited:true`, a chunk that reached the model uncited appears in `unused_chunks`, and a claim with no support is reported as `supported_by: []` and counted, never rendered as though cited; **cost reconciliation** — `cost.total_credits` == `done.credits_deducted` == Σ`calls[].credits` and reconciles to the ledger (SC-006), with `image_tokens` present on a `vision` call; **memory** — each injected memory lists its stamped `access_level` and elisions are counted, never silently dropped; and **section forward-compat** (an unrecognized `section.kind` is accepted, not rejected)
- [ ] T100b [P] [US5] **Panel existence-privacy test** in `backend-python/tests/security/test_debug_panel_leak.py` (SC-001, **release blocker**, sibling to T144/T145): across randomized corpora spanning all 5 levels and both collections, assert that **no** entry in any `funnel[].chunks`, `used[]`, or `grounding.unused_chunks` has `access_level >` the requester's clearance, belongs to another member's personal scope, or belongs to another workspace — including at L5 and for a member demoted mid-session; and that a clearance-removed document is **never nameable** from the panel (counts only), so the debug surface cannot be used as an existence oracle for content the caller may not read. The panel is the one place where "show everything" (SC-005) and "reveal nothing above clearance" (SC-001) pull against each other; this test is where that tension is pinned
- [ ] T100c [P] [US5] Contract test for **live `trace_section` streaming** in `backend-go/tests/contract/query_sse_sections_test.go` (FR-021): `rag_retrieval` arrives on the stream **before `done`** (not only from the `GET`), `grounding` + `cost` arrive after generation, the `GET` afterwards returns the same set, a section delivered by **both** paths is **replaced by `kind` and never duplicated**, and a client ignoring `trace_section` entirely still renders a correct run (the event is additive, never required)
- [ ] T100a [P] [US5] Reusable **stream-UI port conformance** — a shared `StreamUIContract` suite (build the activity tree from `id`/`parentId` under **out-of-order and interleaved** arrival; pair concurrent `tool_start`/`tool_end` by `id`; ignore an unmatched `step_end` without corrupting the tree; attach an orphan `parentId` at root; order sections; reach a terminal summary) run against **`SseStreamSource`** *and* **a second, test-only `FixtureStreamSource`** replaying a **non-RAG** script (a planner spawning two sub-agents with nested tool calls, emitting a `planner` section and **no** `rag_retrieval`) — the suite passing **unchanged** against both is what *proves* the reuse claim rather than asserting it (the stream-UI analogue of T091a's second fixture `Pricer` and T027a's `ParityContract`). Plus a `TraceSectionRegistry` suite (registered kind dispatches; **unregistered** kind → generic fallback; registered renderer whose `accepts()` rejects the payload → **also** generic; `resolve()` never returns `undefined`), a **zero-coupling assertion** (no `bm25`/`rrf`/`rerank`/`mem0`/`clearance`/`credits`/`workspace`/`access_level` token and no `features/`/`lib/`/store import anywhere under `frontend/src/stream-ui/**`), a **chrome-free render** (mount with `chrome={}` → a complete run renders with no billing/clearance/approval affordance and no console error), and a **theme-swap assertion** (render the same run with no `--su-*` set, then under a `:root` overriding `--su-accent`/`--su-surface`/`--su-radius-card`/`--su-font-mono` → **different computed styles from identical markup**, no prop changed, no rebuild; plus a static scan asserting no hard-coded hex/`rgb()`, no Tailwind palette utility, and no literal `rounded-*`/`font-mono` under `stream-ui/**`) in `frontend/tests/contract/stream_ui_ports.test.ts` per [stream-ui-ports.md](./contracts/stream-ui-ports.md) (the conformance gate a future `PlannerSection`/`EvalSection` or a swapped transport is held to)

### Implementation for User Story 5

- [ ] T101 [US5] Implement debug-trace assembly in the LangGraph graph (each node writes its fragment to the `debug` state key — intent, tool, HOT/COLD tier, access-filter count, BM25/vector/RRF/rerank-before/after scores, chunk_type, memory, model → the `rag_retrieval` **section payload**, not a top-level struct) in `backend-python/src/services/agent/graph.py` (extends T073; [agent-graph.md](./contracts/agent-graph.md), [sse-events.md](./contracts/sse-events.md), [stream-ui-ports.md](./contracts/stream-ui-ports.md)). Each retrieval stage writes a **`FunnelStage`** — `in`/`out`/`cutoff`/`duration_ms` plus top-N `ScoredChunk`s **including sub-cutoff near-misses**, so "why isn't my document here?" is answerable — and the funnel **conserves candidates** across stages (T100 fails the build if it doesn't). `ScoredChunk` carries `title`/`preview` so the panel is legible rather than a wall of hashes, and `access_level`, which is **always ≤ the requester's clearance** because the pre-filter runs before scoring; clearance removals are **counts only**, never rows (SC-001). `memory` lists each injected memory with its stamped level plus elision counts. A `rerank`→RRF degradation writes `note` on its stage rather than being smoothed over. A graph that later emits a second detail view (planner, eval) adds **another section**, never a widened struct
- [ ] T101a [US5] Implement **grounding + cost assembly** in `generate` (extends T101): map each answer claim to the chunk ids supporting it (`used[]` with `cited`/`citation_markers`), record chunks that entered the prompt and were **never cited** (`unused_chunks` — usually retrieval was right and the prompt was wrong), and report a claim with no support as **`supported_by: []`**, counted in `unsupported_claim_count` and never silently equivalent to a cited claim: that is the hallucination signal and suppressing it defeats the panel's purpose. Emit the `cost` section from **every gateway call site** (`rewrite`/`rerank`/`generate`/`vision`) with tokens, credits, `duration_ms`, `image_tokens` for vision, and `fallback_hops` — `total_credits` MUST equal `done.credits_deducted` and reconcile to the ledger (FR-021, SC-005, SC-006)
- [ ] T101b [US5] Stream sections **as their stage completes** via the T075 stream adapter — `retrieve` finishing emits `rag_retrieval`, `generate` finishing emits `grounding` + `cost` — so the panel fills during the run instead of only after it (previously the trail streamed live while the drawer stayed empty until `done`). Nodes publish to the transport-agnostic stream channel, **never Redis directly**, keeping the graph extractable ([agent-graph.md](./contracts/agent-graph.md) Streaming). The `GET` endpoint stays authoritative for reloads and returns the same sections; a section arriving on both paths is replaced by `kind`, never duplicated
- [ ] T102 [US5] Surface the `DebugResponse` **envelope** (`{ sections: TraceSection[], summary: RunSummary }` — Phase 1 wraps the retrieval trace as the single `rag_retrieval` section; `langfuse_trace_url` also echoed as `summary.traceUrl`) through the Go `GET /query/{streamId}/debug` handler in `backend-go/internal/query/infra/transport/http/handler.go` (extends T076; [sse-events.md](./contracts/sse-events.md)). The handler MUST pass `payload` through opaquely — it never re-validates retrieval field names, so a new section kind needs no BFF change
- [ ] T103 [P] [US5] Implement the **AISAT retrieval-trace renderer** — `RagRetrievalSection`: the funnel as a **stage-by-stage narrowing** (`9 → 7 → 3` with each stage's `in`/`out`, `duration_ms` and a **drawn cutoff line** so near-misses are visible as near-misses rather than absences), legible chunk rows (`title` + `preview`, not hashes) with an `access_level` chip, the HOT/COLD tier, the structured access-filter counts, chunk expansion, the injected-memory list with per-memory levels and elision counts, and the trace link — registered on the T077 `TraceSectionRegistry` under `kind: "rag_retrieval"`, in `frontend/src/features/chat/trace/RagRetrievalSection.tsx` (FR-021, [stream-ui-ports.md](./contracts/stream-ui-ports.md)). **App** code on the product side of the seam: the generic `AgentTraceInspector` hosting it (T077) knows nothing of retrieval. Renders **incrementally** — the section appears when `retrieve` completes, not at `done`
- [ ] T103a [P] [US5] Implement the **`GroundingSection` + `CostSection` renderers** in `frontend/src/features/chat/trace/` and register both (FR-021). Grounding is the panel's most valuable view: each claim with its supporting chunks, an **unsupported claim rendered as a distinct amber state — never hidden and never styled like a cited one** (it is the hallucination signal), and `unused_chunks` shown as its own list since content that reached the model and contributed nothing usually means the prompt, not retrieval, was at fault. Cost renders the per-call split (`rewrite`/`rerank`/`generate`/`vision`) with tokens, credits, duration and fallback hops, so "why did this cost 42 credits?" is answerable — `image_tokens` is called out because vision cost varies by an order of magnitude with resolution. Both are ordinary registered sections; neither touches `stream-ui/`

**Checkpoint**: Every answer is fully observable; no step hidden (SC-005).

---

## Stage 8: User Story 6 - Admin usage dashboard (Priority: P3)

**Goal**: A workspace admin views per-user/per-feature AI usage, credit consumption, and cost, and manages member limits.

**Independent Test**: Generate usage across members; confirm the dashboard shows per-user/per-feature consumption and cost; adjusting a limit is enforced next op.

### Tests for User Story 6 ⚠️ (write first, must fail)

- [ ] T104 [P] [US6] Contract test for `GET /admin/usage` and `PATCH /workspaces/{id}/members/{userId}` limit enforcement in `backend-go/tests/contract/admin_usage_test.go` (FR-022)

### Implementation for User Story 6

- [ ] T105 [US6] Create `llm_call_log` table + `llm_cost_daily` materialized view migration (add the UNIQUE index required for `REFRESH MATERIALIZED VIEW CONCURRENTLY`; refreshed single-owner in `cmd/worker` via the `usage.matview.refresh` tick, research §15) in `backend-go/migrations/0013_llm_usage.sql` (FR-022, FR-024)
- [ ] T106 [US6] Implement admin usage service (aggregate per-user/per-feature from `llm_cost_daily`, enforce member-limit updates) in `backend-go/kernel/admin/usage.go` (FR-022)
- [ ] T107 [US6] Implement admin HTTP transport (`GET /admin/usage`, member-limit PATCH) + `SetupModule` in `backend-go/kernel/admin/transport/http/handler.go`
- [ ] T108 [P] [US6] Implement admin dashboard UI (per-user/per-feature usage + cost, member-limit controls) in `frontend/src/features/admin/`

**Checkpoint**: Admins can observe and govern usage.

---

## Stage 9: User Story 7 - Long-horizon tasks via a local agent (Priority: P3)

**Goal**: An optional local agent runs multi-step tasks using workspace-scoped tools, routing AI calls through the server by default (metered/audited); long tasks are durable, cancellable, and bounded by a per-task cost cap. All core features work with zero agents.

**Independent Test**: Register an agent, start a long-horizon task, interrupt the worker (resumes from checkpoint), cancel (cancelling→cancelled), and hit the `credits_cap` (halts), all metered/audited.

### Tests for User Story 7 ⚠️ (write first, must fail)

- [ ] T109 [P] [US7] Contract test for `POST /devices/authorize`, `GET/DELETE /devices/{id}`, `POST /llm/proxy` (PAT scope, token budget, credit deduction) in `backend-go/tests/contract/devices_test.go` (FR-025, FR-026)
- [ ] T110 [P] [US7] Contract test for `GET /agent-runs` + `POST /agent-runs/{id}/cancel` (`cancelling`→`cancelled`) and per-run `credits_cap` halt in `backend-go/tests/contract/agent_runs_test.go` (FR-026, FR-028, SC-009); a **BYOK-mode device** starting a long-horizon run is rejected up front with reason `byok_ineligible` **before** any `agent_run` row is written or credits spent (server-routed mode is required — FR-026, spec Clarification 2026-07-23), never a silent runtime failure
- [ ] T110a [P] [US7] Contract test for `/approvals` + resume in `backend-go/tests/contract/approvals_test.go` per [bff-rest.md](./contracts/bff-rest.md) (FR-040, SC-014): `GET /approvals` recipient-scoped; a non-approver `GET`/`resolve` → `404` (existence privacy); `POST /approvals/{id}/resolve` idempotent (first decision wins); approving a `long_horizon_action` publishes `agent.resume.<ws>` and resumes once (no re-spend); no `billing.deduct` while pending
- [ ] T111 [P] [US7] Integration test for long-horizon durability in `backend-python/tests/integration/test_long_horizon.py` (FR-028, SC-009): worker kill → resume from the Redis checkpoint located via the `agent_run.state` pointer (continues from the last completed node, no credit re-spend); stale-heartbeat janitor re-queue; and a pointer whose Redis checkpoint is gone transitions the run to `failed('checkpoint_lost')`, never a silent restart ([agent-graph.md](./contracts/agent-graph.md))
- [ ] T112 [P] [US7] Integration test confirming all core features work with zero connected agents in `backend-go/tests/integration/zero_agent_core_test.go` (FR-025, SC-008)

### Implementation for User Story 7

- [ ] T113 [US7] Create `devices`, `agent_policies`, `agent_audit_log`, `agent_run` migrations (partitioned where noted; `agent_run` checkpoint `state` JSONB, `credits_cap`/`credits_spent`) in `backend-go/migrations/0014_agents.sql` (FR-025–FR-028). `agent_audit_log` carries the tamper-evident chain columns `seq` BIGINT + `prev_hash` + `entry_hash` (per-tenant linked history, the `HashChain` durable form of T022c — distinct from the per-row `result_hash`), per [audit-ports.md](./contracts/audit-ports.md). `agent_policies` includes the narrow Phase-1 write scope: `can_write` BOOL (default false), `write_ops[]` (Phase 1 only `note_update`), `write_max_level` INT ≤ owner clearance (FR-041, SC-015)
- [ ] T114 [P] [US7] Implement device + policy + agent-run models in `backend-go/internal/policy/model/device.go`, `policy.go`, and `agent_run.go`
- [ ] T115 [US7] Implement device registration service (issue scoped 90d PAT, list/revoke, `workspace_id` from PAT only) in `backend-go/internal/policy/service/device.go` (FR-025, FR-027)
- [ ] T116 [US7] Implement LLM proxy + policy chain (`POST /llm/proxy`: authenticate PAT, enforce `allowed_tools`/token budget, deduct credits, resolve alias, forward **as a synchronous HTTP streaming pass-through to the standalone LLM gateway `:4000`** — `stream:true` relays provider SSE verbatim, flush-per-chunk, no buffering; client disconnect cancels `request.Context()` → forwarded HTTP request → gateway stream; not gRPC/NATS, research §20/§21 — then trace; admin-disableable BYOK) in `backend-go/internal/policy/service/llm_proxy.go` (FR-026, FR-027)
- [ ] T117 [US7] Implement agent-run service (cancel propagation, per-step `credits_cap` check independent of daily budget; **long-horizon eligibility gate** — a run started from a **BYOK-mode** device is refused at submission with reason `byok_ineligible` **before** any `agent_run` row or spend, since the server-side worker cannot make AI calls with a key it does not hold and long-horizon requires server-routed mode; a BYOK agent may still drive its own loop via `/llm/proxy` + MCP) in `backend-go/internal/policy/service/agent_run.go` (FR-026, FR-028, SC-009)
- [ ] T118 [P] [US7] Implement policy repository + the AI-tool-call audit producer (`tool_called`→`Action`, `token_cost`/`trace_id`→`Attributes`, `result_hash`→`ResultHash`, `Actor{Kind:"agent", Role:agent_role}`) recorded through the T022c `Recorder`/`Sink` port (`agent_audit_log` is the `PostgresSink` partition) rather than a hand-rolled `INSERT`, in `backend-go/internal/policy/infra/repo/policy_repo.go` (FR-023, [audit-ports.md](./contracts/audit-ports.md))
- [ ] T119 [US7] Implement policy HTTP transport (`/devices/*`, `/llm/proxy`, `/agent-runs/*`) + `SetupModule` in `backend-go/internal/policy/infra/transport/http/handler.go` and `backend-go/internal/policy/module.go`
- [ ] T119a [US7] Implement the BFF approvals transport — `GET /approvals`, `GET /approvals/{id}`, `POST /approvals/{id}/resolve` (recipient-scoped via the T022b `ApprovalStore`; idempotent resolve; `404` for a non-approver) — and publish `agent.resume.<ws>` on approve/edit of a `long_horizon_action` gate, in `backend-go/internal/policy/infra/transport/http/approvals.go` (wired in the policy `SetupModule`, T119) (FR-040, SC-014, [bff-rest.md](./contracts/bff-rest.md), [nats-subjects.md](./contracts/nats-subjects.md))
- [ ] T120 [US7] Implement Python long-horizon worker in `backend-python/src/services/agent/long_horizon.py`: the **durable compiled form** of the T073 graph (`graph.compile(checkpointer=RedisSaver)`, AOF) with bounded checkpointed re-entry, heartbeat every 10s, cancel handling, and per-run cap halt. Persist `agent_run.state` as the checkpoint **pointer** (`{thread_id, checkpoint_ns, checkpoint_id, node, step}`), resume by loading the authoritative Redis checkpoint for `thread_id`, and transition to `failed('checkpoint_lost')` if the pointer's checkpoint is gone — never a silent restart (SC-006). The stale-heartbeat re-queue runs in a **single-owner janitor role** triggered by the external `agent.janitor.tick` (not an in-process timer per pod) and re-queues via a conditional `UPDATE agent_run SET status='queued' WHERE id=$1 AND status='running' AND last_heartbeat_at < $2 RETURNING id` so concurrent janitors re-queue each run once (FR-028, SC-009, research §15, [agent-graph.md](./contracts/agent-graph.md))
- [ ] T120a [US7] Implement the `human_gate` graph node + resume path in `backend-python/src/services/agent/graph.py` and the long-horizon worker (T120): in the **durable compiled form only**, `human_gate` opens the approval via `deps.gate`, calls `interrupt(payload)` (checkpoints, sets `agent_run.status='paused'`, **no spend while paused**), and on the `agent.resume.<ws>` consumer loads the paused thread's checkpoint and resumes via `Command(resume=decision)` **exactly once** past the gate (no re-spend); a `reject` short-circuits (`block_reason='approval_rejected'`, index not mutated). The decision is the injected human value, never tool output (FR-040, SC-014, [agent-graph.md](./contracts/agent-graph.md), [approval-ports.md](./contracts/approval-ports.md))
- [ ] T120b [US7] Implement the **`edit_note`** Category-D write tool (FR-041, SC-015) in `backend-python/src/mcp_server/tools/edit_note.py` and wire the `web_search_decide` + `edit_note` steps into the **durable** graph form (extends T073/T120a): `edit_note` proposes a diff to an **existing** note, authorized by the Go Authorizer `Permit(ActionUpdate)` + `WriteEnvelope` with `Clearance=min(agent,owner)` (own personal + workspace ≤ clearance; denies above-clearance as `not_found`, denies `envelope_widens`, refuses create), gated by the human-gate (`approval_request(kind='note_edit')`); on approve the note `body` is updated and re-indexed (chunk→embed) and metered `operation_type='note_edit'`, on reject it is unchanged. Requires `agent_policies.can_write` + `note_update ∈ write_ops`. `web_search_decide` sets `needs_web`/`pending_approval` and routes through `human_gate` before each fetch ([mcp-tools.md](./contracts/mcp-tools.md), [agent-graph.md](./contracts/agent-graph.md), [authorizer-ports.md](./contracts/authorizer-ports.md))
- [ ] T121 [P] [US7] Implement device-management + agent-run UI (register/revoke device, run list, cancel; for a **BYOK-mode** device, render long-horizon as an **ineligible option with the `byok_ineligible` reason** shown inline rather than a control that fails at run time — FR-026, spec Clarification 2026-07-23) in `frontend/src/features/admin/components/Devices.tsx` and `AgentRuns.tsx`
- [ ] T121a [P] [US7] Implement the approvals UI — a pending-approvals inbox and an inline approve/reject/edit control that renders the live `approval_request` SSE event mid-run (paused → resolve → resumed) — in `frontend/src/features/approvals/` (FR-040)

**Checkpoint**: All 7 user stories independently functional; local agents are additive and fully metered/audited.

---

## Stage 10: User Story 8 - Stay informed through notifications (Priority: P3)

**Goal**: Recipient-scoped notifications for ingestion, invites, credit warning/exhaustion, task-halt, doc-shared, clearance-change, member-joined, and admin broadcast — persisted to an in-app inbox, pushed in real time over SSE, and (per opted-in category) delivered by email via a provider-agnostic port. Each member controls delivery per category × per channel.

**Independent Test**: Trigger an event for a recipient, confirm the in-app notification arrives in real time and increments the unread badge; mark read and confirm the badge decrements; disable a category's email channel and confirm a later event sends no email while still appearing in-app; confirm a second member never sees the first member's notifications.

### Tests for User Story 8 ⚠️ (write first, must fail)

- [ ] T129 [P] [US8] Contract test for `/notifications` list (`?unread=`, pagination), `/notifications/unread-count`, `/notifications/{id}/read` (404 for non-recipient), `/notifications/read-all`, `GET/PUT /notifications/preferences`, and `POST /admin/notifications/broadcast` in `backend-go/tests/contract/notifications_test.go` per [bff-rest.md](./contracts/bff-rest.md) (FR-032–FR-037)
- [ ] T130 [P] [US8] Contract test for `/notifications/stream` SSE taxonomy (initial `unread_count`, then `notification` + `unread_count` per event) in `backend-go/tests/contract/notifications_sse_test.go` per [sse-events.md](./contracts/sse-events.md) (FR-034)
- [ ] T130a [P] [US8] **Delivery-latency assertion** for SC-011 in `backend-go/tests/integration/notification_latency_test.go`: over N≥50 `notify.<ws>` events to a connected recipient, assert the **p95 elapsed time from event publish to the `notification` frame arriving on `/notifications/stream` is < 5s**, and that the `unread_count` frame accompanying it reflects the new total without a re-fetch (FR-034, **SC-011**). The same budget is exported as an OTel histogram (`notify.delivery.latency_ms`) so the target is measurable in production, not just in test (Principle IX)
- [ ] T131 [P] [US8] Integration test for recipient scoping — member A never receives/sees member B's notifications, and no cross-workspace leakage even at L5 (RLS) in `backend-go/tests/integration/notification_scoping_test.go` (FR-036, SC-012)
- [ ] T132 [P] [US8] Integration test for preference + channel fan-out: disabled email channel publishes no `notify.email.<ws>`; simulated email-provider failure routes to `notify.email.dlq.<ws>` while in-app delivery succeeds in `backend-go/tests/integration/notification_email_test.go` (FR-035)
- [ ] T132a [P] [US8] Integration test for idempotent + crash-safe delivery — the same `notify.<ws>` event delivered twice (same `idem_key`) yields exactly one persisted notification, one in-app push, and one `notify.email.<ws>` publish; **and** a crash after the persist tx commits but before dispatch loses no delivery: the outbox is drained on recovery and still yields exactly one email (closes the `SET NX`-short-circuit hole) in `backend-go/tests/integration/notification_idempotency_test.go` (FR-032, FR-035, SC-013)
- [ ] T132b [P] [US8] Integration test for coalescing — a burst of same-category events for one recipient produces a digest/rate-limited summary rather than one push + one email per event in `backend-go/tests/integration/notification_coalesce_test.go` (FR-038)
- [ ] T132c [P] [US8] Integration test for email suppression + unsubscribe — a send to a suppressed address is skipped (not retried); a valid unsubscribe token disables that category's `email` channel; an unsigned `/webhooks/email/{provider}` body is rejected in `backend-go/tests/integration/notification_suppression_test.go` and `backend-go/tests/contract/notifications_webhook_test.go` (FR-035)
- [ ] T132d [P] [US8] Contract/integration test for async broadcast — `POST /admin/notifications/broadcast` returns promptly, fans out per-recipient off the request path, and is audited in `backend-go/tests/integration/notification_broadcast_test.go` (FR-037)
- [ ] T132e [P] [US8] Integration test for `notify.email.dlq` drain via `dlq.sweep.tick` — a genuinely-unsent parked email is re-driven to `notify.email.<ws>` and delivered exactly once (mark-sent only after provider-accept; suppressed address skipped, not re-driven); an email reaching `MAX_DLQ_ATTEMPTS` lands in `dead_letters` with a `dlq.dead.count` emit and is not re-driven again in `backend-go/tests/integration/notification_dlq_drain_test.go` (research §18, FR-035)
- [ ] T132f [P] [US8] Reusable **port contract tests** — a shared `ChannelContract` suite every `Channel` impl must pass (idempotent on `idem_key` under re-drive; `Suppressed` is terminal not retryable; transient failure returns `Retryable`) run against both the `in_app` and `email` channels, plus a `NotifierContract` suite (idempotent persist; recipient-scoping leak check, SC-012; crash-after-persist-before-dispatch loses no delivery) in `backend-go/tests/contract/notification_ports_test.go` per [notification-ports.md](./contracts/notification-ports.md) (the conformance gate a future SMS/push/Slack channel is held to)

### Implementation for User Story 8

- [ ] T133 [P] [US8] Implement notification + preference models in `backend-go/internal/notification/model/notification.go` and `preference.go` (categories, priority, payload, read state)
- [ ] T133a [P] [US8] Implement the `ChannelRegistry` + the two Phase-1 `Channel` implementations behind the `kernel/notify` ports — `in_app` (Redis pub/sub `notify:user:<id>`) and `email` (the `kernel/mailer.go`-backed sender, owning suppression + unsubscribe) — plus the `TopicRegistry` (the 13 categories — incl. `approval_requested`, FR-040/FR-041 — as registered topics with default channels/priority) and a `TemplateRenderer`, so the Dispatcher (T134a) fans out over `for kind := range enabled { registry.Get(kind).Deliver(...) }` and a third channel (SMS/push/Slack) is a `Register` call, not a fan-out edit, in `backend-go/internal/notification/channel/{registry,in_app,email}.go` + `topics.go` per [notification-ports.md](./contracts/notification-ports.md)
- [ ] T134 [US8] Implement notification service (consume `notify.<ws>` as a **JetStream queue-group consumer hosted in `cmd/worker`** so it scales out independently per-subject; resolve recipient prefs with category defaults; then in **one DB transaction** persist the `notifications` row **and one `notification_outbox` row per enabled channel** — idempotent via `(user_id, idem_key) UNIQUE`. The `SET NX notify:applied:{idem_key}` guard is a fast pre-check on the **durable write only**, never on channel delivery, so a crash before dispatch cannot drop an email) in `backend-go/internal/notification/service/notify.go` (FR-032–FR-035, SC-013; [notification-ports.md](./contracts/notification-ports.md) invariants 3–4)
- [ ] T134a [US8] Implement the notification **Dispatcher** (queue-group consumer in `cmd/worker` on `notify.<ws>`/outbox lag) that drains `notification_outbox` **at-least-once**: for each pending entry, deliver via `ChannelRegistry.Get(entry.channel).Deliver(...)` (T133a) — `in_app` → `PUBLISH notify:user:<id>` (badge recomputed from the store on SSE connect); `email` → republish to `notify.email.<ws>` — then mark `delivered_at`, or bump `attempts`/`next_attempt_at` under backoff and park after `MAX_DLQ_ATTEMPTS`; each channel idempotent on `idem_key` so a re-drive is one delivery, in `backend-go/internal/notification/service/dispatcher.go` (FR-032, FR-034, SC-013, research §18, §23)
- [ ] T135 [P] [US8] Implement inbox + preference repository (list/unread-count/mark-read/mark-all, prefs upsert) in `backend-go/internal/notification/infra/repo/notification_repo.go` (FR-033, FR-035)
- [ ] T136 [US8] Implement notification HTTP + SSE transport — inbox REST (`/notifications/*`) wired in `cmd/api`, and `/notifications/stream` relaying `notify:user:<id>` wired in `cmd/relay` (connection-bound, scales with relay replicas) — plus `/admin/notifications/broadcast` + `SetupModule` in `backend-go/internal/notification/infra/transport/http/handler.go` and `backend-go/internal/notification/module.go` (FR-033, FR-034, FR-037)
- [ ] T137 [US8] Wire producers to publish `notify.<ws>` events (each stamping a deterministic `idem_key` from the originating resource + event, SC-013) from existing flows (ingestion complete/failed, invite received/accepted/revoked, credit warning/exhausted, agent-run cost-cap halt, **approval requested** — a human-gate opened awaiting the member, FR-040/FR-041, in the T022b approval service — doc shared, clearance change, member joined) in their respective services (FR-032)
- [ ] T138 [P] [US8] Implement the Go email worker — the transport+provider half of the `email` `Channel` (T133a) that runs in `cmd/worker`, consumes `notify.email.<ws>` — using the `kernel/mailer.go` port with a default Resend adapter (env-swappable), `html/template` rendering with an unsubscribe link, `email_suppressions` skip-check, `idem_key`/`notification_id` dedup that marks an address **sent only after provider-accept** (so a DLQ re-drive of a genuinely-unsent mail still delivers, exactly once), retry with backoff, and `notify.email.dlq.<ws>` parking (stamping `dlq_attempts`/`first_failed_at`) in `backend-go/internal/notification/service/email_worker.go` and `backend-go/internal/platform/mailer/resend.go` (FR-035, research §18)
- [ ] T139 [P] [US8] Implement notification bell + inbox + per-category/per-channel preferences UI (live SSE badge, mark read/all, deep-link via payload) in `frontend/src/features/notification/`
- [ ] T140 [US8] Implement async admin-broadcast fan-out — enqueue one job from `POST /admin/notifications/broadcast` that publishes per-recipient `notify.<ws>` events off the request path, audited in `backend-go/internal/notification/service/broadcast.go` (FR-037)
- [ ] T141 [US8] Implement same-category coalescing/digest (rate-limited summary per recipient over a short window) in the notification service in `backend-go/internal/notification/service/coalesce.go` (FR-038)
- [ ] T142 [P] [US8] Implement notification retention job (prune/archive read notifications older than the configured window, default 90d; optional range-partition drop on `created_at`), triggered via the scheduler seam (`notify.retention.tick`) in `backend-go/internal/notification/service/retention.go` and wired in `cmd/worker` (FR-039)
- [ ] T143 [US8] Implement signature-verified email-provider webhook `POST /webhooks/email/{provider}` (upsert `email_suppressions` on bounce/complaint) and one-click `GET /notifications/unsubscribe` (signed token → disable category `email` channel) in `backend-go/internal/notification/infra/transport/http/webhook.go` (FR-035)

**Checkpoint**: All 8 user stories independently functional; notifications are recipient-scoped, real-time in-app, and email-deliverable per preference.

---

## Stage 11: Polish & Cross-Cutting Concerns

**Purpose**: Reliability, observability, retention, evaluation gate, and final validation across all stories

- [ ] T122 [P] Configure provider one-hop fallback + multi-key load-balancing + circuit breaker (never on low-quality output) in the **standalone LLM gateway** (`deploy/llm-gateway/config.yaml`, LiteLLM router; Bifrost-swappable) and export `llm.fallback.count`; verified via the gateway, not application code (FR-029, research §21)
- [ ] T123 [P] Implement the canonical PII scrubber in `backend-python/src/services/pii_scrub.py` (single implementation; the LLM gateway T026 is its only caller, covering both primary and one-hop-fallback calls) applied before any trace/eval write + 30-day raw-body retention via partition `DROP` job in `backend-go/migrations/0015_retention_jobs.sql` (FR-024, Clarification Q5)
- [ ] T124 [P] Wire workspace/member-action audit producers (invite/admin/policy/approval writers, `Actor{Kind:"user"}`, `security` severity) through the T022c `audit` `Recorder` (tamper-evident `HashChain` seal, `PostgresSink` over `audit_log`) rather than a standalone inline writer, in `backend-go/kernel/audit/` and the calling services (FR-023, [audit-ports.md](./contracts/audit-ports.md))
- [ ] T125 Implement Phase 1 eval seed set + runner (`evals/run.py`: ≥20 prompt cases; `prompts/retrieval/eval.py`: ≥30 golden queries) with the hard access-filter assertion (query at level N never returns doc > N) in `backend-python/evals/run.py` and `backend-python/prompts/retrieval/eval.py` (FR-030, SC-002, SC-003). **Includes the SC-017 clarification-rate gate**: across the golden set the system requests clarification on **≤ 10%** of queries, and a clarified follow-up answers at least as well as the unclarified baseline — so **over-asking fails the build** rather than passing as caution. Without a measured ceiling, "ask when unsure" drifts toward asking constantly, which degrades the product as surely as guessing wrong
- [ ] T126 [P] Wire CI gates into `Makefile`/CI: lint+format (gofmt/golangci-lint, ruff/black, eslint/prettier), ≥80% coverage per runtime, Testcontainers integration runs (`go test -tags=integration` / `pytest -m integration`), Playwright E2E, performance/bundle-size checks, security scan, and the Phase 1 eval gate
- [ ] T127 [P] Implement Playwright E2E suite for the critical journeys (upload→indexed library; ask→cited streamed answer + debug panel; access-scoped visibility for two clearances; near-limit warning + exhaustion block) in `frontend/tests/e2e/` (SC-004, SC-005, SC-008, SC-010). The upload→cited-answer journey **asserts the SC-004 budget** — first upload to first cited answer completes in **< 5 minutes**, capturing the measured elapsed time as the test's evidence so the criterion is enforced, not merely referenced
- [ ] T128 Execute [quickstart.md](./quickstart.md) validation Scenarios 1–8 end-to-end and record evidence (Principle X)

### Access-control adversarial hardening (SC-001, release blocker)

> SC-001 is a release blocker at "100%". Functional happy-path tests are insufficient — these property-based and adversarial tests cover **every** surface that touches authorized content: Qdrant retrieval, semantic cache, Mem0 memory, and Postgres RLS. The two non-negotiable gates are the deny-by-default filter guard (T144) and the zero-above-clearance eval assertion (T148, extending T125).

- [ ] T144 [P] Property-based retrieval filter tests in `backend-python/tests/security/test_retrieval_filter_property.py` (Hypothesis): across randomized corpora over all 5 levels × both collections, assert no returned chunk has `access_level > requester_clearance` or a foreign `workspace_id`; assert a member B query never returns member A's personal-collection chunk even at L5; assert the **deny-by-default guard** (T027) raises when a search is attempted with no `workspace_id`/`access_level` filter (FR-007, SC-001)
- [ ] T145 [P] Cross-clearance semantic-cache adversarial tests in `backend-python/tests/security/test_cache_cross_clearance.py`: same normalized query at L4 then L2 → L4 populates, L2 is a **miss** and never receives L4 bytes; same clearance but different `user_id` for personal-scoped answers → also a miss; inject a unique sentinel into the L4 answer and assert it never appears in any L2 response across N runs (FR-007, SC-001, research §2)
- [ ] T146 [P] Memory temporal/adversarial tests in `backend-python/tests/security/test_memory_clearance.py`: extends T061a into a property test over write-clearance × read-clearance pairs — assert a memory is injected at Node 5 iff its stamped `access_level` ≤ the requester's current clearance, and a sentinel L4 fact is never surfaced after demotion (FR-009, SC-001, research §13)
- [ ] T147 [P] RLS negative tests in `backend-go/tests/security/rls_negative_test.go`: a tenant query executed without `SET LOCAL app.workspace_id` returns 0 rows / errors (never the full table); a forged `workspace_id` in the request body is ignored (resolved server-side) and RLS blocks cross-workspace reads (FR-014, SC-001)
- [ ] T148 Extend the Phase 1 eval gate (T125) with the **hard zero-above-clearance assertion** as a blocking CI check: across the full golden query set, no above-clearance labeled doc ID appears in any answer's citations or retrieved set; non-zero violations fail the build (FR-030, SC-001)
- [ ] T149 [P] Reusable **approval port conformance** — the `ApprovalContract` suite (per [approval-ports.md](./contracts/approval-ports.md)) every `HumanGate`/`ApprovalStore` impl must pass, across all Phase-1 kinds (`enrich_accept`, `long_horizon_action`, `sensitivity_confirm`, `web_search`, `note_edit`): `Create` idempotent (one live gate/subject); `Resolve` idempotent (first decision wins); **recipient-scoping leak check** — a non-approver gets `not_found`, never the gate (SC-014, release blocker, parity with SC-012); `Expire` fails closed; zero spend while pending; decision provenance is human-only. Go store suite (Testcontainers PG) in `backend-go/tests/contract/approval_ports_test.go`; the graph half (interrupt → no-spend → resume-exactly-once → no re-spend; reject never mutates the index; `note_edit` never commits above clearance / above the source floor / as a create — SC-015) in `backend-python/tests/integration/test_human_gate.py` (FR-040, FR-041, SC-014, SC-015)

---

## Dependencies & Execution Order

### Stage Dependencies

- **Setup (Stage 1)**: No dependencies — start immediately
- **Foundational (Stage 2)**: Depends on Setup — **BLOCKS all user stories**
- **User Stories (Stages 3–10)**: All depend on Foundational completion
  - US1 (P1) and US2 (P1) form the MVP; US2 retrieval/agent depends on US1 ingestion having indexed content for meaningful end-to-end tests, but both can be developed in parallel against fixtures
  - US3–US8 can proceed in parallel once Foundational is done (if staffed)
- **Polish (Stage 11)**: Depends on the targeted user stories being complete

### User Story Dependencies

- **US1 (P1)**: After Foundational — no dependency on other stories
- **US2 (P1)**: After Foundational — consumes US1's indexed chunks at runtime; independently testable via seeded fixtures
  - **Profile-B runnable path (built in Phase 1)**: the pgvector store bootstrap (T034a) and the Python `Bus` port (T029a, Foundational) precede the `RetrievalService` pgvector backend (T065a); T065a + T029a are the production impls the swap test (T060d) binds; the single-container entrypoint (T060e) and its live smoke (T060f) close the loop. All are additive — Profile A (Qdrant + JetStream) is unaffected because both backends sit behind the same ports.
- **US3 (P2)**: After Foundational — independently testable; strengthens access scoping US1/US2 already honor
- **US4 (P2)**: After Foundational — metering hooks into US1/US2 AI ops; independently testable
- **US5 (P2)**: After US2 (extends the query graph + debug endpoint)
- **US6 (P3)**: After Foundational — reads `llm_call_log` populated by US2/US4
- **US7 (P3)**: After Foundational — reuses credits (US4) + MCP tools (US2); core works with zero agents (SC-008)
- **US8 (P3)**: After Foundational — consumes events produced by US1/US3/US4/US7 flows; independently testable via a directly published `notify.<ws>` event; recipient-scoping is a release blocker (SC-012)

### Within Each User Story

- Tests written first and FAIL before implementation (Principle VI)
- Models → services → endpoints → integration
- Migrations precede repositories that read the tables

### Parallel Opportunities

- All Setup tasks marked [P] run in parallel
- All Foundational [P] tasks within their subsection run in parallel
- Once Foundational completes, US1–US8 can be staffed in parallel
- All [P] test tasks within a story run in parallel (different files)
- All [P] models within a story run in parallel

---

## Parallel Example: User Story 1

```bash
# Launch all US1 tests together (write first, must fail):
Task T035: "Contract test for POST /ingest/presign in backend-go/tests/contract/ingest_presign_test.go"
Task T036: "Contract test for /notes + enrich SSE + accept + status in backend-go/tests/contract/ingest_test.go"
Task T037: "Contract test for ingestion NATS subjects in backend-python/tests/contract/test_ingestion_subjects.py"
Task T038: "Integration test for ingestion pipeline in backend-python/tests/integration/test_ingestion_pipeline.py"
Task T039: "Contract test for /documents in backend-go/tests/contract/documents_test.go"

# Then launch parallel converters/captioner/distiller:
Task T049: "MarkItDown converter in backend-python/src/services/ingestion/markitdown.py"
Task T050: "Image captioner in backend-python/src/services/ingestion/captioner.py"
Task T051: "Shared web_distill capability in backend-python/src/services/ingestion/web_distill.py"
Task T051a: "Note-enrich worker in backend-python/src/services/ingestion/enrich.py"
```

---

## Implementation Strategy

### MVP First (User Stories 1 + 2)

1. Complete Stage 1: Setup
2. Complete Stage 2: Foundational (CRITICAL — blocks all stories; includes RLS, LLM gateway, MCP server, access filter)
3. Complete Stage 3: US1 (ingest → library)
4. Complete Stage 4: US2 (ask → cited, access-scoped answer)
5. **STOP and VALIDATE**: quickstart Scenarios 1, 2, 4 (ingest→answer, access scoping, injection refusal)
6. Deploy/demo the core knowledge loop

### Incremental Delivery

1. Setup + Foundational → foundation ready
2. US1 + US2 → MVP knowledge loop → demo
3. US3 → multi-tenant access control → demo
4. US4 → credit metering/budgets → demo
5. US5 → debug observability → demo
6. US6 → admin dashboard → demo
7. US7 → optional local agents / long-horizon → demo
8. Polish → reliability, retention, eval gate, full quickstart validation

### Parallel Team Strategy

1. Whole team completes Setup + Foundational together
2. Once Foundational is done:
   - Dev A: US1 (ingestion)
   - Dev B: US2 (retrieval/agent) against fixtures
   - Dev C: US3 (workspace/access) + US4 (credits)
3. US5 follows US2; US6/US7 pick up after Foundational as capacity allows

---

## Notes

- [P] = different files, no incomplete-task dependencies
- [Story] label maps each task to its user story for traceability
- Tasks tagged *(Phase 2)* (T026c–T026g) are forward-compatibility seams — excluded from the Phase-1 Definition of Done; they land with the k3s/EKS `transform_files` write-back work, not the Phase-1 exit gate
- Access control is enforced at the data layer (Postgres RLS + Qdrant payload pre-filter) in Foundational and every story — never by prompt (SC-001, release blocker)
- Verify tests fail before implementing (Red→Green→Refactor, Principle VI)
- Integration tests provision real Postgres/Redis/NATS/Qdrant via Testcontainers; critical journeys are covered by Playwright E2E (never mocked infra for these layers)
- Claim a task done only with verification evidence (Principle X)
- Commit after each task or logical group
- Stop at any checkpoint to validate a story independently
